# PC 登录验证码视觉验收

- 参考问题截图：`/private/var/folders/yn/8j0yr4gs2pzg508td1q42cv00000gn/T/codex-clipboard-08334f61-1845-4a5e-b96d-0895149d3b22.png`
- 实现截图（密码登录）：`/private/tmp/qixi-pc-login-inline-captcha-final.png`
- 实现截图（验证码登录）：`/private/tmp/qixi-pc-login-puzzle-captcha.png`
- 视口：1280 × 720 CSS px，设备像素比 1。
- 状态：本地 `http://127.0.0.1:5183/login`；验证码服务走 `api-business` BFF，语言 `zh-CN`。

## 对比结论

原问题是密码登录将图形验证码做成二次弹窗，并把 SDK 的英文取消错误直接渲染到表单；验证码登录拼图窗口宽度为 840px，明显超过桌面登录流程所需的操作范围。

本次实现：

1. 密码登录在表单第三行直接显示亮色图片验证码和输入框；无独立刷新按钮，点击验证码图片即可刷新，图片完整等比显示。
2. 图形验证码请求明确传递 `preferred_mode: image`、`theme: light`、`locale: zh-CN`。
3. 验证码登录使用白色拼图验证，并收缩为最大 460px 宽、52px 滑块的紧凑弹窗。
4. 主动关闭拼图验证只结束当前验证，不再显示 `Captcha cancelled` 等英文内部错误。

## 验证记录

- [x] 浏览器实测密码登录页面已直接显示图片验证码。
- [x] 浏览器实测切换验证码登录后显示紧凑拼图窗口；未求解或绕过 CAPTCHA。
- [x] 关闭验证码窗口后，登录表单没有英文取消错误文案。
- [x] `pnpm -C app-pc type-check` 通过。
- [x] 浏览器控制台无 error/warn。
- [x] `git diff --check` 通过。

## 质量结论

- 字体与层级：与原 PC 登录弹窗的中文标题、表单层级保持一致。
- 间距与布局：验证码在原有表单行内完成，不增加二次窗口；拼图窗口不再覆盖主要页面区域。
- 色彩：图片验证码使用亮色背景；拼图验证使用白色弹窗与蓝色滑块。
- 图片：使用验证码服务返回的 PNG，等比完整显示，未裁剪字符。
- 文案：所有用户可见验证码文案为中文，取消行为不显示内部英文错误。

final result: passed

---

# PC 全页视觉与交互闭环验收（进行中）

## 约束

- 登录、注册及验证码的 UI 与交互已锁定；本轮不改动，仅允许后续按需微调字体和间距。
- 视觉基线来自 `screenshot/pc/` 中的 CRMEB 页面截图；交互基线来自仓库已实现的真实接口与状态机，不以静态占位替代。

## 本轮同视口首屏对照

对照尺寸为 1280 × 720 CSS px。每个页面均把 `screenshot/pc/` 的参考图与同尺寸本地截图拼接后人工检查；临时对照产物位于 `/private/tmp/qixi-pc-qa/`，不纳入仓库。

| 页面 | 参考 | 视觉状态 | 已实际验证的交互 | 结论 |
| --- | --- | --- | --- | --- |
| 首页 | `首页.png` | 半透明分类栏、轮播、商品区结构已对齐；装修接口未返回时使用同一组图片的首屏回退，避免白屏 | Banner 切换、分类入口、分类楼层懒加载与“加载更多” | 首屏通过；长页仍待逐段验收 |
| 全部商品 | `全部商品.png` | 已压缩店铺推荐与筛选区，首行商品卡进入首屏 | 分类筛选、价格排序、价格区间查询与分页参数 | 首屏通过 |
| 店铺首页 | `店铺页面.png` | 左侧店铺信息与右侧商品栅格已对齐 | 商品卡与店铺导航使用真实商品/店铺读取接口 | 首屏通过，店铺收藏需登录态验收 |
| 店铺优惠券 | `店铺优惠卷.png` | 店铺信息侧栏、券卡、领取按钮结构已对齐 | 未登录领取明确跳转登录并保留回跳地址 | 匿名流程通过；已领取状态待登录态验收 |
| 领券中心 | `领劵中心.png` | 已使用独立的真实背景资源、筛选栏与双列券卡 | 全部/通用券/店铺券筛选；领取受 JWT 保护 | 匿名浏览通过；领取成功待登录态验收 |
| 秒杀 | `秒杀页面.png` | 已替换为浅色活动背景资源，场次卡、商品卡层级与参考一致 | 00:00/07:00/14:00/19:00 场次切换 | 首屏与场次切换通过 |
| 新闻中心 | `新闻中心.png` | 左侧资讯列表与右侧最新资讯层级已对齐 | 点击资讯跳转 `/notices/2005` 详情页 | 首屏与详情跳转通过 |
| 拼团 | 暂无对应截图 | 商品图卡与活动价已补齐 | 列表读取、详情入口 | 匿名浏览通过；真实下单待登录态验收 |
| 预售 | 暂无对应截图 | 商品图卡、定金/尾款标识、活动横幅已补齐 | 列表读取；未登录下单受真实认证和地址保护 | 匿名浏览通过；真实下单与尾款支付待登录态验收 |
| 好友助力 | 暂无对应截图 | 商品图卡、助力人数、活动横幅已补齐 | 列表读取、详情入口 | 匿名浏览与详情通过；开团/助力待登录态验收 |
| 社区种草 | 暂无对应截图 | 商品图、话题标签和内容卡片已补齐 | 话题筛选（`通勤穿搭` 返回 1 条）、详情入口 | 匿名浏览、筛选与详情通过；发帖/评论待登录态验收 |
| 预约服务 | 暂无对应截图 | 服务商品图卡、日期与时段面板已补齐 | 3 个服务列表、7 天日期、2 个可用时段读取 | 公开选期通过；创建预约、取消释放和店员核销待登录态验收 |
| 购物车 | `购物车.png` | 参考图为已登录且有商品状态；当前匿名态正确显示登录保护 | 匿名“去登录”保护 | 阻塞：需合法登录后的真实购物车数据 |
| 提交订单 | `提交订单.png` | 参考图为已登录且有地址、商品、券与发票状态 | 匿名访问不创建订单 | 阻塞：需合法登录后的完整下单链路 |
| 商户入驻 | `申请入住.png` | 当前页面有真实申请提交与历史查询，但参考图中的短信验证码、执照上传控件尚未等价实现 | 未登录会回跳登录 | 阻塞：短信服务与 COS 上传配置未闭环，不能伪造成功 |
| 账户/地址/余额/积分/发票/收藏/我的优惠券/订单/售后 | 对应 `我的*.png`、`账户管理.png`、`收货地址.png` | 参考图均为已登录状态 | 匿名入口均保持登录保护，无浏览器错误 | 阻塞：不能绕过 CAPTCHA 或注入令牌来伪造认证态验收 |

## 已修复的接口读取缺陷

直播页此前从 `qixi_crm_b_store_view` 读取不存在的 `merchant_name` 列，导致公开直播读取失败。现改为从 `qixi_crm_b_product_view` 的店铺投影获取商户名，并在本地业务测试数据中加入 3 个公开直播间。浏览器已验证 `/live` 展示直播中、未开始、已结束三种状态且控制台无错误；直播详情在未配置播放地址时明确显示“直播播放地址暂未配置”，不会伪造直播流。

## 已核验项

- [x] `pnpm -C app-pc type-check` 通过。
- [x] `pnpm -C app-pc build` 通过。
- [x] `go test ./internal/business/live ./internal/pkg/middleware ./internal/business/catalog` 通过。
- [x] `git diff --check` 通过。
- [x] 浏览器核验首页、全部商品、店铺、店铺优惠券、领券中心、秒杀、新闻、购物车、结算页及直播列表；本轮记录的公共页面控制台无 error。
- [x] 浏览器核验预售、助力、社区和预约公开页面；预售/助力/社区各有 3 条带图夹具，预约有 3 个服务及可读排期。
- [x] `go test ./internal/domain/reservation ./internal/infra/persist/reservation ./internal/domain/trade ./internal/app/reservation` 通过。

## 仍未闭环的系统性阻塞

1. 拼团、预售、助力、社区与预约的公开读模型已迁入 `qixi_crm_b_*`；预约时段余量改为 `qixi_crm_b_reservation_booking` 账本读取，不再扫描历史 `qixi_m_app` 订单。**但**通用交易订单、支付、取消和退款主链路仍有历史订单模型依赖，未完成统一迁移前不能标为生产就绪。
2. 预约创建现在会以时段行锁和预约账本校验防止同一时段并发超卖；取消订单、退款后的时段释放及店员核销尚未完成同一条状态机闭环，因此不能把预约支付链路标为已验收。
3. 已登录状态的页面不能通过绕过 CAPTCHA、伪造 JWT 或写浏览器存储来验收；需要使用本地真实账户完成正常登录后，继续测试购物车、结算、地址、发票、资产、订单与售后。
4. 商户入驻的短信验证码和营业执照上传依赖尚未配置真实短信通道与 COS 上传服务；在未配置前仅能拒绝或提示，不应假装可完成。

final result: blocked

---

# PC 首页 Banner 与展示类型视觉验收

- 参考截图：`/var/folders/yn/8j0yr4gs2pzg508td1q42cv00000gn/T/codex-clipboard-3646ea85-380f-41eb-aea3-98e479009857.png`
- 实现状态：本机 PC 首页，1280 × 720 CSS px。
- 对比状态：桌面端首页首屏；左侧半透明分类栏、顶部导航、Banner、轮播箭头与页码处于可见状态。

## 对比与结论

1. 保留参考图的白色导航、红色“商品分类”标题、深色半透明分类栏与大图 Banner 结构；Banner 使用真实图片资源，不使用 CSS 绘制占位。
2. 轮播切换采用淡入淡出与轻微缩放过渡；左右箭头避开右侧快捷栏，已验证点击不会误跳转商品页。
3. 页面下滑时，推荐区、当季新品、榜单和分类展示区逐段进入；分类商品首次进入视口才请求数据，并提供“加载更多商品”分页入口。
4. 平台装修器的“商品组 → 自动获取 → 分类”成为客户端展示类型来源；未配置时仅回退为“服饰鞋包、家居生活”。测试 SQL 将两个默认分类首屏条数设为 4，以便继续分页展示。

## 验证记录

- [x] 浏览器实测轮播包含 3 张 Banner，点击“下一张 Banner”后仍位于首页且图片已切换。
- [x] 浏览器实测下滑后“服饰鞋包”“家居生活”两区按分类加载对应商品。
- [x] 浏览器截图与参考截图完成桌面首屏视觉比对；分类栏、Banner 层级、导航间距和页码位置无裁切或遮挡。
- [x] `pnpm -C app-pc build` 通过。
- [x] `go test ./internal/business/catalog` 通过。
- [x] `git diff --check` 通过。

final result: passed

---

PC 全页视觉与交互闭环仍在逐页执行；以本文件最后结论为准。

final result: blocked
