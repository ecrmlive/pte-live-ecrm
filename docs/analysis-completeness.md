# 功能解析完整度说明

**当前标准（按你的要求）**：功能点必须落到各端、具体功能、按钮级，增删改查尽量齐全。

权威清单入口：[`docs/features/README.md`](./features/README.md)

## 1. 已落实

| 端 | 粒度 | 文档 | 操作数（约） |
| --- | --- | --- | ---: |
| 平台后台 | 页面 → 按钮权限（不足则路由补全） | `features/01-platform-admin.md` | 1295 |
| 商户后台 | 同上 | `features/02-merchant-admin.md` | 600 |
| 用户端 | API 路由 = 操作；附录小程序页 | `features/03-user-app.md` | 342 |
| 商户手机端/店员 | API 路由 = 操作 | `features/04-merchant-mobile.md` | 95 |
| 客服端 | API 路由 = 操作 | `features/05-customer-service.md` | 18 |
| OpenAPI | API 路由 = 操作 | `features/06-openapi.md` | 6 |
| 总表 | TSV 可检索 | `generated/features-master.tsv` | 2409 |

每条操作带 CRUD 分类（C/R/U/D/O）。后台页面有 CRUD 覆盖一览表。

## 2. 尚未清零（阻塞「基线锁定」）

见 [`features/08-gaps.md`](./features/08-gaps.md)：

| 类型 | 数量 | 含义 |
| --- | ---: | --- |
| 无任何操作的页面 | 53 | 必须补全或标明「仅目录/外链」 |
| CRUD 不完整的页面 | 169 | 需业务判断（只读页可无 C/D；业务页应补齐） |

另需人工确认：

1. 用户端：**页面按钮文案** 与 API 操作的一一对应（现为接口级完整）。  
2. 配送员 / 服务人员独立能力是否都落在 `manager` / 用户 API 中。  
3. 功能表 PNG 中「标准多商户版」红字项是否全部出现在菜单/路由清单中（抽检对照）。

## 3. 与「可以写代码」的关系

| 条件 | 状态 |
| --- | --- |
| 各端按钮/操作级清单已生成 | ✅ |
| 缺口清单已列出 | ✅ |
| 缺口已清零并经你确认 | ❌ |
| 功能基线锁定 | ❌ |
| 允许写业务代码 | **否**（默认） |

## 4. 建议你怎么验收

1. 打开 `features/01`、`02`，抽查「商品管理 / 订单列表 / 店铺入驻」是否每个按钮都在。  
2. 打开 `features/03`，抽查地址 CRUD、下单、支付、退款是否都有独立操作行。  
3. 处理 `08-gaps.md` 的 53 个空页。  
4. 回复「功能基线锁定」后，再进入技术设计/编码。
