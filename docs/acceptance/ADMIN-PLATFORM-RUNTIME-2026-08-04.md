# 统一后台隔离运行时验收：2026-08-04

本轮仅使用本地临时 MySQL 8.4.10、临时服务端口与虚构账号；未部署、未使用真实凭据、未连接真实第三方服务。

| 验收项 | 结果 | 可复核事实 |
| --- | --- | --- |
| MySQL 8.4 导入兼容 | 通过 | 修复商户商品表增量 DDL 的 `ADD COLUMN IF NOT EXISTS` 不兼容写法，改为仓库既有的 `information_schema` 条件 DDL；同时为三库 `02_data.sql` 增加 `SET NAMES utf8mb4`。临时核验菜单标题字节为 `E68EA7E588B6E58FB0`，三库架构与中文夹具均可导入。 |
| 平台登录与 Vben | 通过 | 平台虚构账号登录后进入控制台；页面显示中文仪表盘、店铺销售及待办数据。 |
| 发票监管 | 通过 | 平台查询到 1 条虚构发票，税号与邮箱均脱敏；商户、区域、运营、客服请求均为 HTTP 403。 |
| 操作日志 | 通过 | 平台查询到 2 条操作日志，页面只展示管理员、角色、路由资源、请求号和时间；四个非平台角色请求均为 HTTP 403。 |
| 登录日志 | 通过 | 五个虚构角色登录后，平台页面可查询 8 条日志，含本次五角色登录；四个非平台角色请求均为 HTTP 403。 |
| 文章入口回归 | 通过静态验证 | DIY 文章组件、专题链接与控制台快捷入口均已改为真实 `/cms/article` 页面，`admin-platform/src/views/ecrm` 不再引用旧 `/plus/article` 或 `/content/article` 占位路径；Vben 构建通过。 |
| 客服越权入口回归 | 通过静态验证 | 客服工作台已移除“客服人员管理 → 管理员管理”跳转；客服保留队列、快捷回复及客服人员/服务用户能力。“客服设置”只对平台角色展示，避免向客服展示服务端必然拒绝的全局队列策略入口。Vben 构建与差异检查通过。 |
| 平台优惠券发送记录 | 通过 | 平台查询 2 条不可变发送审计，按撤销操作筛选为 1 条；页面显示中文操作原因、状态变化和只读边界。四个非平台角色访问均为 HTTP 403。 |
| 平台优惠券领取记录 | 通过 | 平台查询 3 条用户券，按已使用状态筛选为 1 条；页面显示未使用、已使用、已过期状态，不提供人工修复。四个非平台角色访问均为 HTTP 403。 |
| 动态菜单中文显示 | 通过 | 修复三库基础数据种子的字符集声明；重新加载真实 Vben 页面后，侧栏、面包屑和控制台中文显示正常，无需前端编码绕行。 |
| 前端存储隔离 | 通过 | 为 Vxe 已浏览行与偏好初始化的 localStorage 管理器增加独立命名空间，避免清理或枚举时波及同域其他应用的数据；Vben 构建和重载后的浏览器日志通过。 |
| SVIP 五角色与主数据 | 通过 | 平台、运营可读取套餐/权益/购买记录；商户、区域、客服对四个 SVIP 端点均为 HTTP 403。运营新增、编辑、逻辑删除未引用中文权益成功；旧版本更新返回 409。 |
| SVIP 关联与订单只读 | 通过 | 被启用套餐引用的“会员专享价”删除返回 409；停用后创建引用该权益的套餐返回 400，恢复后运营可创建“验收专用体验会员”。订单统计固定为总计 2、待支付 1、已支付 0、已关闭 1、已支付金额 ¥0.00，未伪造支付成功。 |
| SVIP Vben 页面 | 通过 | 平台登录后展示中文“会员类型”“会员记录”页面、套餐权益、状态筛选和只读统计；记录状态显示为“待支付 / 已关闭”，不直接暴露内部状态码。 |
| 后台会话恢复 | 通过 | 修复历史 JWT 401 被误判为网络错误以及前端错误调用 `/api/v1/auth/refresh` 的问题；登录后存储独立 refresh token，整页重载后继续停留会员记录页并成功刷新会话。 |
| 普通会员等级配置 | 通过 | 平台虚构账号可读取、创建、更新、逻辑删除普通会员等级；商户、区域、客服、运营对配置、候选等级与人工调整接口均为 HTTP 403。重复排序与旧版本更新分别返回 409；已分配或已有历史日志的等级删除返回 409，逻辑删除等级不能再被调整。 |
| 普通会员等级调整 | 通过 | 虚构用户完成“悦享会员→尊享会员→普通会员”命令闭环；相同幂等键回放不产生重复日志，不同内容复用键返回 409。直接查询确认留存两条不可变日志，积分 268、余额 ¥36.50、佣金 ¥0.00 保持不变。Vben 中文等级管理、调整页及登录后深层地址直达均已截图复验。 |
| 用户搜索记录五角色隔离 | 通过 | 平台查询到 3 条 UTF-8 中文记录，按“夏季”筛选为 1 条；商户、区域、运营、客服对读取、清理、导出三类接口均为 HTTP 403。页面只展示用户 ID、关键词、来源、时间，不回传账号、手机号或设备标识。 |
| 搜索记录清理与导出审计 | 通过 | 平台对虚构用户逻辑清理 2 条记录；同键重放保持清理数 2，不同请求复用键返回 409，清理后用户记录不可见。按“儿童”导出 1 条 UTF-8 BOM CSV；库内核验 2/3 条逻辑删除、清理审计 1 条（数量 2）、导出审计 1 条（行数 1）。Vben 截图复验中文操作与本地可读时间。 |
| 用户资产调账五角色隔离 | 通过 | 平台可执行余额和积分人工调账；商户、区域、运营、客服请求均为 HTTP 403。余额采用固定分计算：¥36.50 增加 ¥0.10 后为 ¥36.60；积分 268 增至 270。小数积分返回 400，超额余额扣减返回 409。 |
| 用户资产幂等与审计 | 通过 | 同键重放不重复写账；同键改金额或中文原因均返回 409。业务库直接核验平台人工账本与审计各 2 条、幂等键唯一，账户佣金保持 ¥0.00；中文审计原因按 utf8mb4 存储。Vben 页面截图确认高风险提示、余额/积分选择和原因必填。 |
| 用户优惠券五角色隔离 | 通过 | 平台读取当前有效的虚构“服饰店9折券”并执行发券/撤券；商户、区域、运营、客服对模板读取、发券和撤券端点均为 HTTP 403。Vben “用户优惠券操作”页截图显示模板选择、中文原因必填及锁定/已使用/过期状态不可人工修改提示，浏览器控制台无错误。 |
| 用户优惠券并发、幂等与状态边界 | 通过 | 虚构用户 9101 对同一模板的两个同键并发发券恰有一次创建、一次重放；改中文原因复用发券键返回 409，重复发券返回 409。撤券后状态为 `expired`，撤券同键重放不重复写入，改中文原因返回 409；已使用券撤销与过期券再次发放均返回 409。最终审计 2 条（发券/撤券各一条）、键唯一，中文原因 utf8mb4 正常存储。 |
| 用户推荐关系五角色隔离 | 通过 | 虚构平台账号可调整上级关系；商户、区域、运营、客服对同一写接口均为 HTTP 403。Vben 页面截图确认中文循环约束、留空清除上级、原因必填和历史佣金/订单事实不重算提示，浏览器控制台无错误。 |
| 用户推荐关系并发、幂等与循环边界 | 通过 | 虚构用户 9101 以同键并发从上级 9001 调整至 9002，恰有一次创建、一次重放；改中文原因复用键返回 409，随后可清除上级且同键重放不重复写审计。9002→9101 的循环命令返回 409；9001→9002 与 9002→9001 反向并发时仅一条成功、另一条循环拒绝。最终关系图无环，审计 3 条且键唯一，中文原因 utf8mb4 正常存储。 |
| 用户分组归属五角色隔离 | 通过 | 平台读取虚构“新品体验用户”有效分组并可批量调整；商户、区域、运营、客服对分组读取和批量写入均为 HTTP 403。Vben 截图确认 1–100 用户输入、中文分隔符、未分组清除和业务事实隔离提示，浏览器控制台无错误。 |
| 用户分组归属并发、原子性与审计 | 通过 | `[9001,9999]` 与不存在目标分组均返回 HTTP 404，未产生部分写入；同键并发的重复数组规范化后恰有一次创建、一次重放，改中文原因返回 409。9001、9002 完成“新品体验用户→未分组”闭环，9101 的既有分组未变；审计 2 条、键唯一，中文原因 utf8mb4 正常存储。 |
| 用户标签归属五角色隔离 | 通过 | 平台读取虚构“高频复购用户”“香氛兴趣用户”等有效标签并可整体替换/清除；商户、区域、运营、客服对标签读取和批量写入均为 HTTP 403。Vben 截图确认整体替换、留空清除、中文输入和业务事实隔离提示，浏览器控制台无错误。 |
| 用户标签归属并发、原子性与审计 | 通过 | `[9001,9999]` 与不存在目标标签均返回 HTTP 404，未写入关系或审计；重复用户/标签、不同数组顺序的同键并发命令规范化后恰有一次创建、一次重放，改中文原因返回 409。9001、9002 完成“两枚标签→清除”闭环，9101 既有标签保持不变；审计 2 条、键唯一，中文原因 utf8mb4 正常存储。 |
| 用户启停五角色隔离与幂等 | 通过 | 隔离 MySQL 中创建“中文状态验收用户”；平台令牌可执行停用，商户、区域、客服、运营令牌均为 HTTP 403。八个同键并发停用请求仅一次创建、七次重放，命令审计仅一条；改变中文原因复用幂等键返回 HTTP 409。 |
| 用户启停与 C 端会话失效 | 通过 | 用户完成 `status=1/auth_version=1 → 0/2 → 1/3`。停用时 C 端身份版本 1、2 的受保护请求均为 HTTP 401；重新启用后版本 1、2 仍为 HTTP 401，版本 3 为 HTTP 204。测试令牌仅在本地进程内签发，不输出、不落盘。 |
| 用户启停 Vben 页面 | 通过 | 本地 Vben 以虚构“中文启停验收平台主管”登录并打开“用户启停”；截图确认用户 ID、停用/启用、中文原因、会话失效及不撤销交易事实提示均渲染，浏览器仅有 Vite 连接调试日志，无错误。 |
| 用户密码重置五角色与并发边界 | 通过 | 隔离 MySQL 中以“中文口令验收账号”创建 PC 身份；平台可执行重置，商户、区域、客服、运营请求均为 HTTP 403。六个同键并发请求仅一次成功、五次 HTTP 409；审计仅一条且不保存口令或其派生摘要。 |
| 用户密码重置与 C 端会话失效 | 通过 | 虚构初始口令的 bcrypt 校验失败，虚构新口令校验通过，用户 `auth_version` 从 1 升至 2。C 端身份版本 1 的受保护请求为 HTTP 401，版本 2 为 HTTP 204；测试令牌仅在本地进程内签发，验收数据随后清理。 |
| 用户新增与资料维护五角色及并发边界 | 通过 | 平台以虚构中文账号创建本地 PC 用户；商户、区域、客服、运营对创建和资料维护均为 HTTP 403。五个同键并发创建仅一次成功、四次冲突；不同幂等键创建相同账号返回冲突。资料同键并发为一次执行、四次重放，改中文原因复用键返回 HTTP 409。昵称、`/demo/` 头像、性别和中文简介整体原子保存。 |
| 用户创建/资料/密码重置 Vben 页面 | 通过 | 修复 `user.create` 页面码与按钮码冲突后，以虚构中文平台管理员登录，分别打开“新增用户、用户资料维护、用户密码重置”并截图；三页的中文表单、密码安全边界、公开资料边界及原因输入均正确显示，干净浏览器会话控制台无错误。 |
| 批量设置推广员资格 | 通过 | 两个虚构中文用户完成“开通→停用”资格闭环；商户、区域、客服、运营请求均为 HTTP 403。部分集合含不存在用户为 HTTP 404 且无资格写入；六个同键并发开通仅一次执行、五次重放，改中文原因返回 HTTP 409。既有佣金账本固定为 ¥18.88、`available`，未被资格变更修改；Vben 页面截图与控制台检查通过。 |
| 用户站内图文通知 | 通过 | 平台虚构账号可向隔离中文假用户发送 C 端站内图文通知；商户、区域、客服、运营均为 HTTP 403，不存在收件人为 HTTP 404，非受控封面为 HTTP 400。六个同键并发仅创建一条通知和一条审计、其余五次重放，改中文原因复用键为 HTTP 409；通知载荷为 `image_text`、`/demo/` 中文封面、未读状态。Vben 表单发送后显示审计成功提示，截图和控制台检查通过；不调用或模拟任何外部公众号、短信、邮件、IM 服务。 |
| 商户保证金退款打款幂等 | 通过 | 临时 MySQL 中创建虚构商户保证金账户（余额 ¥500.00）与两笔中文退款申请。首笔以“本地模拟打款凭证-001”登记成功后，余额为 ¥400.00、版本为 2、退款流水仅一条；相同命令重放成功但不重复扣款。用相同幂等键改为“本地模拟篡改凭证-002”，以及将该键用于第二笔退款，均为 HTTP 409，第二笔保持 `approved`。夹具已自动清理。 |
| 商户保证金扣减幂等 | 通过 | 临时 MySQL 中以余额 ¥500.00 的虚构商户执行“中文模拟违规扣减”¥100.00。相同命令重放不重复扣款；同键改为 ¥50.00 或改中文原因均为 HTTP 409；¥0.001 返回 HTTP 400。账户最终余额 ¥400.00、版本 2，`deduct` 流水仅一条；夹具已自动清理。 |
| 商户分账申请并发审核 | 通过 | 临时 MySQL 中创建不含渠道账户资料的“中文模拟分账申请”。6 个并发“审核通过”请求为 1 次 HTTP 200、5 次 HTTP 409；申请最终为 `approved`，只生成一条 `applied→approved` 审计。更新内部中文备注后，审计仍保留原审核说明；501 字中文备注请求返回 HTTP 400。夹具已自动清理。 |
| 商户财务五角色 HTTP/RBAC | 通过 | 临时创建五个中文后台验收账号并使用测试进程临时 JWT 访问真实注册路由。平台在会话版本、按钮权限验证后读取保证金/分账申请均为 HTTP 200；商户、区域、运营、客服均为 HTTP 403；缺少令牌为 HTTP 401。账号角色关系与夹具均已自动清理。 |
| 商户财务 Vben 页面与前置校验 | 通过 | 临时 API 与 Vben 连接隔离三库，虚构平台账号登录后真实打开“店铺保证金”“店铺分账申请”页面并截图。保证金的 ¥0.001 输入在提交前显示“精确到分”校验；分账空审核说明被前置拦截；取消两类提示框后，干净浏览器会话控制台无 warning/error。临时账号将在本轮结束时删除。 |
| 商户分类 CRUD、五角色与 Vben | 通过 | 临时 MySQL 中完成中文分类 CRUD：新增返回真实 ID，重复名 HTTP 409，不存在更新/删除 HTTP 404，三位小数佣金 HTTP 400。同构 JWT/RBAC 链中平台读取为 HTTP 200，商户、区域、运营、客服均为 HTTP 403。真实 Vben 完成“中文商户分类验收页面→已编辑→删除”并截图，列表中文与 9.25% 佣金正确显示，控制台无 warning/error；夹具与虚构账号均将在本轮结束时清理。 |
| 店铺分组 CRUD、树、五角色与 Vben | 通过 | 临时 MySQL 中完成 UTF-8 中文三级树、状态级联、模板绑定和商户投影校验；同级重名返回 HTTP 409，缺失模板为 HTTP 400、缺失分组为 HTTP 404。生产同构 JWT/RBAC 链中平台读取为 HTTP 200，商户、区域、运营、客服均为 HTTP 403，未登录为 HTTP 401。真实 Vben 完成“中文分组页面验收→已编辑→关联七禧演示店铺→查看关联→停用→删除”闭环；取消模板弹框后浏览器仅有 Vite 调试日志，无 warning/error。夹具与虚构账号已清理。 |
| 店铺类型 CRUD、资金精度、五角色与 Vben | 通过 | 临时 MySQL 中完成 UTF-8 中文店铺类型的详情、新增、编辑、备注、启停和删除；启用保证金为零或分以下金额均为 HTTP 400，类型重名为 HTTP 409，不存在更新/删除为 HTTP 404，删除后菜单授权关系同步清理。生产同构 JWT/RBAC 链中平台读取为 HTTP 200，商户、区域、运营、客服均为 HTTP 403，未登录为 HTTP 401。真实 Vben 完成“中文类型页面验收→已编辑→备注→停用→删除”，创建时绑定商品管理菜单和 ¥199.50 虚构保证金；浏览器应用控制台无 warning/error。夹具与虚构账号已清理。 |
| 商户保证金余额不足与退款审核竞争 | 通过 | 临时 MySQL 以 UTF-8 中文夹具验证余额 ¥400.00 时扣减 ¥401.00、以及已批准退款 ¥500.00 的打款登记均返回 HTTP 409，余额 ¥400.00、版本 2 和原流水保持不变。六个“通过/驳回”并发审核仅一次 HTTP 200、五次 HTTP 409；成功请求同事务写一条 `refund_approved` 或 `refund_rejected` 不可变流水，审核说明必填且不超过 500 字。夹具已自动清理。 |
| 商户入驻受控命令与平台监管投影 | 通过（分库事务验收） | 隔离 MySQL 使用“七禧入驻并发验收茶铺”“王小明”等 UTF-8 虚构数据：商户库 8 个并发开通命令仅落 1 个商户、1 个店铺和 1 个 owner 账号，重复命令返回同一商户/店铺；平台库同一待审申请的 6 个并发投影仅 1 次成功、5 次“已审核”拒绝，最终申请为 `approved` 且 `qixi_crm_a_merchant_view` 中文字段、区域和状态正确。夹具已自动清理。该项不替代尚未执行的临时 NATS 端到端与 C 端申请回写联调。 |
| 商户入驻 NATS 通信契约 | 通过（组件间验收） | 临时 `nats:2.12.0-alpine` 中实际启动 `api-merchant` 入驻订阅器，以“七禧NATS入驻验收花店”“李华”等 UTF-8 命令完成 request/reply；重复命令返回同一商户和店铺。平台入驻客户端独立通过同一 subject 的中文请求/响应契约测试。临时 NATS、MySQL 与夹具均已清理；尚未将完整平台审核处理器和 C 端申请 outbox 串成同一次运行。 |
| 区域代理资格撤销 | 通过 | 隔离 MySQL 与真实 Vben 使用虚构“七禧区域代理页面验收”完成撤销确认，中文原因触发受控软撤销，页面显示“已撤销”。已撤销行仅保留只读关联商户入口；待审行只保留编辑，已通过行才显示重置密码和撤销，避免向用户展示服务端必然拒绝的操作。五角色 HTTP 链中仅平台为 200，商户、区域、运营、客服均为 403，未登录为 401；代理、审计、临时账号、API/Vite 会话均将在本轮清理。 |
| 区域代理后台密码重置 | 通过 | 隔离 MySQL 与真实 Vben 使用“七禧代理密码页面验收”和虚构绑定区域账号，平台填写两次一致的新密码及中文原因后提示旧会话已失效；库内只核验 `auth_version=2`、bcrypt 格式与 1 条无口令审计，不输出或保存口令。五角色路由链中平台为 200，商户、区域、运营、客服均为 403，未登录与重置前旧令牌均为 401；浏览器控制台只有 Vite 调试日志。临时账号、代理、审计、API 与 Vite 均将在本轮清理。 |
| 客服工作台：队列、快捷回复与转接 | 通过 | 隔离 MySQL 使用 UTF-8 中文虚构店铺、用户与会话：平台可领取并转接会话，Vben 完成快捷回复“新增→编辑/停用→软删除”与中文转接原因闭环，页面显示转接后的目标客服。五角色真实 JWT 链中：平台可更新全局客服设置；商户、区域、运营、客服更新均为 HTTP 403；客服仅看到授权店铺会话，可维护授权店铺快捷回复和备注，范围外会话详情/备注均为 HTTP 404、范围外快捷回复写入为 HTTP 403。转接同键重放不重复写审计，同键改原因返回 HTTP 409。修复历史无队列客服返回 `service_store_ids:null` 导致 Vben 转接筛选异常的问题，契约统一为 `[]`，并以新浏览器会话确认控制台只有 Vite 调试日志。临时账号、会话、快捷回复、API/Vite 与 MySQL 均将在本轮清理。 |
| 积分商城监管 | 通过 | 隔离 MySQL 以 UTF-8 中文虚构商品、商户和关闭订单完成：运营可更新 120 积分/库存 5/上架商品，平台旧版本写入返回 409，商户、区域、客服均为 403；商品更新后订单的积分、件数和 `closed` 状态保持快照。C 端报价在商品下架后拒绝新建。真实 Vben 平台账号将“中文积分页面商品”从 120/8 更新为 130/7，并以“已关闭”状态筛选到只读订单；浏览器控制台无 error。临时账号、商品、订单、API/Vite 与 MySQL 均将在本轮清理。 |
| 用户提现审核与打款登记 | 通过 | 隔离 MySQL 以 `WD-UI-中文-001` 的 ¥66.50 虚构提现完成平台审核后登记内部模拟凭证，状态由 `approved` 到 `paid`；同一键同一凭证安全重放，改凭证重放返回 409。商户、区域、运营、客服读取和登记均为 403。Vben 真实页面只显示“已脱敏收款账户”，登记后显示“已打款”，浏览器控制台无 error；未调用任何外部付款渠道。临时账号、申请、登录记录、API/Vite 与 MySQL 均将在本轮清理。 |
| 预售活动监管 | 通过 | 隔离 MySQL 的“中文预售页面验收商品”以定金 ¥59.00、尾款 ¥240.00、预售价 ¥299.00、库存 12 完成详情与启用→停用 Vben 操作；停用后报价拒绝。平台/运营可访问，商户、区域、客服均被真实路由拒绝。接口只允许 `status`，价格或库存写入不会修改活动事实；页面控制台无 error。临时账号、活动、登录/操作审计、API/Vite 与 MySQL 均将在本轮清理。 |
| 好友助力活动监管 | 通过 | 隔离 MySQL 的“中文好友助力页面验收商品”以助力价 ¥199.00、库存 12 完成详情与上架→下架 Vben 操作；隐藏后 C 端不能再发起助力。平台/运营可访问，商户、区域、客服被真实路由拒绝。接口只允许 `is_show`，混入价格或库存不会改写活动事实；页面控制台无 error。临时账号、活动、登录/操作审计、API/Vite 与 MySQL 均将在本轮清理。 |
| 分销推广与佣金监管 | 通过 | 隔离 MySQL 的业务读模型以虚构中文后台账号、推广员关系及佣金 6.50/12.80/3.20 验证平台/运营可读，商户、区域、客服被真实路由拒绝；非法佣金状态为 HTTP 400。真实 Vben 正确显示汇总、推广员和佣金流水，且不提供佣金、提现或关系修改操作；浏览器无 error。临时账号、关系、流水、API/Vite 与 MySQL 均将在本轮清理。 |
| 商户结算审核与打款 | 通过（NATS 跨服务与 Vben） | 临时 NATS、商户库与平台库以“七禧结算页面验收茶铺”虚构账单完成 `withdraw_applied → approved → paid`；平台审核和内部模拟凭证登记均由受控 request/reply 命令执行，商户 outbox 发布后平台只读投影异步刷新。真实 Vben 显示 ¥1280.50、已审核与已打款，控制台无 error。修复同一幂等键改审核动作、操作人或打款凭证会被静默重放的问题；临时账号、商户、店铺、账单、投影、NATS 和 API 均已清理。 |
| 商户结算订单事实链 | 通过（隔离三库 NATS） | 虚构“七禧结算跨服务应计茶铺”与“七禧结算跨服务退款花店”分别完成业务 outbox `accrue/reverse → accepted`、商户不可变账本 `+¥188.50/-¥42.75`、商户 outbox、平台只读投影 `bill_pending` 的异步闭环；平台投影正确显示负向调整。另以真实 Gin 确认收货处理器验证订单 `shipped → completed` 与 `settlement:accrue:<order_id>` 同事务写入且重放不重复；账本 NATS 集成测试验证账期冻结后退款只生成当期负向账单，历史冻结账单不变。修复账本重放查询缺失表模型、负数投影被拒绝，以及幂等键未严格绑定订单/退款事实三个缺陷。所有中文夹具以 utf8mb4 核验，临时资源随后清理。 |
| Sandbox 退款终态、库存与结算 | 通过（隔离三库 NATS） | 临时业务服务的 sandbox-only 回调以 `refund_id` 触发，未接收金额、店铺、SKU 或渠道资料。虚构“七禧退款跨服务验收茶铺”完成 `refunding → refunded`、订单 `aftersale → cancelled`、主单/支付 `paid → refunded`；业务库存/结算 outbox 均 `accepted`，商户库存账本将 SKU 从 9 回补至 10，商户结算账本与平台投影均为 `-¥66.50`。相同回调重放后回调、退款事件、库存命令、结算命令、库存账本和结算账本计数均为 1；中文商户名以 utf8mb4 字节核验。未调用真实支付渠道。 |
| 退款监管五角色与区域范围 | 通过（隔离三库 HTTP） | 虚构“七禧退款区域内茶铺 / 区域外花店”及两笔中文退款通过真实注册路由验收：平台列表可见两笔；商户与区域仅见所属区域退款、猜测范围外退款 ID 返回 404；运营、客服与未配置范围账号均为 403；商户和区域发起审批仍为 403。区域路由仅放行范围处理器已覆盖的退款接口，测试结束后账号、角色关系、范围、订单、退款和商户均为 0。 |
| 退款监管 Vben 状态页与空榜防线 | 通过 | 真实 Vben 使用虚构平台账号显示 UTF-8 中文退款原因、¥66.50 和“退款处理中”；操作列只有详情/日志，无同意/拒绝，浏览器控制台无错误。验收中发现空排行接口将切片序列化为 `null`，导致仪表盘读取 `.length` 异常；已令后端返回 `[]` 且前端兼容历史 `null`，新会话显示“本月暂无已支付订单”且无控制台错误。截图仅保存在本机临时路径，环境清理后不保留账号或数据。 |
| 退款日志与导出审计 | 通过（隔离三库 HTTP） | 平台账号读取一条中文退款状态日志、按 `applied` 导出两笔退款；CSV 返回字段标题，不含退款原因，且业务库仅写入一条 `row_count=2`、中文导出原因和平台操作人 ID 的导出审计。商户、区域、运营、客服的日志和导出请求均为 403；夹具清理后后台账号、退款、日志和导出审计计数均为 0。 |
| 退货退款同次跨服务闭环 | 通过（隔离双服务 HTTP） | 同一 UTF-8 虚构退款单以真实注册路由依次完成：C 端 `return-shipment`（C 端 JWT/会话）→ 商户 `confirm-return`（店铺 JWT、`X-AppId`、会话和 `refund.approve`）→ sandbox `refund/mock` → 同回调重放，四次均为 HTTP 200。最终退款/订单/主单/支付为 `refunded/cancelled/refunded/refunded`；`七禧演示快递`物流、退款交易、回调、三条事件、库存与结算 outbox 计数均为 1。`TestReturnShipmentHTTPIntegration` 与 `TestConfirmReturnHTTPIntegration` 同时覆盖重放、篡改和跨归属拒绝；临时库、进程、令牌与数据均已清理，未使用真实凭据或渠道。 |
| 商户退款备注与受控隐藏 | 通过（隔离双库 HTTP） | 复用生产同构店铺 JWT、`X-AppId`、会话及 `refund.approve/refund.remark/refund.delete` 菜单权限。中文备注同键重放不重复写入、修改同键内容为 409、跨店为 404；中文隐藏命令同键重放不重复写入、修改同键内容为 409，隐藏后本店详情为 404。商户库备注与隐藏事实各一条，业务库退款仍为 `refunding`、退款交易仍一条，未删除或改写任何支付、退款或监管事实；清理后四项计数均为 0。 |
| 客服队列、转接与五角色范围 | 通过（隔离双库 HTTP；Vben 已实测） | 虚构“中文客服张敏/李岚/周岚”、两家中文店铺及两个用户经生产同构统一后台 JWT、会话、角色总闸和客服队列范围进入真实路由。平台更新客服设置为 200；商户、区域、运营、客服写设置均为 403。客服仅读取授权店铺会话，范围外详情/用户备注为 404，范围外快捷回复创建/查询为 403、更新为 404；授权快捷回复完成“新增→编辑停用→软删除→列表隐藏”，库内保留操作者和删除时间。领取后同范围转接成功，同键重放不重复写审计、改中文原因返回 409，第三名非当前领取客服转接为 403；同一虚构用户在两家店铺的备注按 `(user_id, store_id)` 独立保存、互不覆盖。真实 Vben 以虚构平台账号显示“七禧客服页面验收店”、中文会话和受控事件抽屉；抽屉仅显示订单/系统事件并声明不保存聊天正文。浏览器临时沙盒截图未能导出为可复核文件，因此 Vben 截图证据仍待补；夹具、账号、API 和 Vite 均已清理。 |
| 区域/商户订单监管范围 | 通过（隔离三库 HTTP；Vben 平台页） | `TestOrderHTTPRBACAndRegionalScope` 在 admin/business/merchant 三库以区域 10/20 的“七禧区域十茶铺/区域二十花店”、两笔中文虚构订单验证真实统一后台 JWT、会话、角色总闸、商户直管范围与区域映射。平台列表可见两笔；商户和区域 `{10}` 只见区域 10，范围外详情为 404；运营、客服及无范围商户均为 403。`paid=0/1` 筛选、授权订单的中文商品明细与虚构物流快照均回读正确；平台订单路由只读，POST 未注册（404）。一次性 Vben 假凭据环境复验“订单监管”页面，显示两笔 UTF-8 中文虚构订单、已支付、待发货/待收货、¥100/¥50、两个详情按钮及只读提示；浏览器控制台无 error。测试与页面夹具均在结束后清理。 |
| 五角色控制台汇总与排行 | 通过（隔离三库 HTTP；Vben 平台页） | `TestDashboardHTTPFiveRoleScopeAndRank` 在 admin/business/merchant 三库以区域 10/20 的两家中文店铺及订单、退款、会话、浏览、关注、审核夹具验证 `/dashboard/summary`。平台得全量 `scope=all`；商户、区域 `{10}`、客服店铺 `{1}` 均为 `scope=store`，只能得到店铺 1 的统计、待办、浏览/访客和排行；运营及无范围商户为 403。店铺 A 有两名关注用户，但预聚合关注数后订单仍只累计一次（¥100、销量 2），防止联表放大销售金额。一次性 Vben 假凭据平台页显示新增用户/浏览量/访客数各 3、店铺数 2，以及 UTF-8 中文店铺排行（关注 2、销量 2、¥100.00；关注 1、销量 1、¥50.00）；浏览器控制台无 error。测试与页面夹具均在结束后清理。 |
| 管理员逻辑删除与客服接续 | 通过（隔离双库 HTTP） | 在同一生产同构路由链中，商户、区域、运营、客服删除虚构“中文客服李岚”均为 403，平台删除自身为 409；平台删除目标客服为 200。删除后数据库只保留历史 ID，账号 `status=0`、认证/范围版本均从 1 递增至 2，旧 JWT 为 401，客服名册不再返回目标，目标不能再成为转接目标（400）。平台将其遗留会话转交“中文客服张敏”成功，已有转接审计仍保留一条目标 ID；UTF-8 夹具和两库数据均自动清理。Vben 截图仍待补。 |
| 成功操作审计与失败拒绝 | 通过（隔离双库 HTTP 与真实 Vben） | 同一测试在真实全局 `AuditAdminMutation` 中间件下验证：平台成功删除虚构客服仅写一条操作日志，记录平台操作人、`DELETE /api/platform/v1/setting/admins/:id`、`setting/987672006` 和非空请求号；四种非平台删除被拒绝且不写审计。平台按 action 查询该日志为 200，商户、区域、运营、客服查询日志均为 403；返回内容不含密码散列或请求体。一次性本地 Vben 环境另实测成功客服设置更新，操作日志页显示 `PUT /api/platform/v1/customer-service/settings`、`customer-service`、虚构请求号与 1 条结果，内联截图复验页面未呈现请求体或凭据。账号、队列、会话和审计夹具在结束后均为 0。 |
| 商品监管范围与审核投影 | 通过（隔离三库 HTTP） | `TestProductHTTPRBACAndRegionalScope` 以区域 10/20 的“七禧区域十茶铺/区域二十花店”、两件 UTF-8 待审商品测试真实 JWT、会话、角色总闸、区域白名单和 `product.audit.submit`。平台列表可见两件；直管商户与区域角色只见区域 10 商品，猜测区域 20 商品 ID 为 404；运营、客服读取为 403，四种非平台角色审核均为 403。平台审核后商户商品变为 `on_sale`，且状态变化与 `qixi_crm_m_product_audit_outbox` 在同一商户事务提交；平台审核事实、业务商品视图和 SKU 消费视图按 source event 幂等投影。测试强制将两层 outbox 标为失败并删除业务视图，重投后视图恢复、两层 attempts 均为 2。隔离库还重复执行了建表脚本，夹具清理后后台账号、审核、商品和两层投影计数均为 0。列表契约只返回商品 ID、商户/店铺与详情摘要，不返回不必要的商品标题；Vben 截图仍待补。 |

该记录只关闭上述已验证功能的隔离运行时证据，不替代订单、支付、退款、库存、优惠、积分、佣金和结算等高风险状态机的生产验收。
