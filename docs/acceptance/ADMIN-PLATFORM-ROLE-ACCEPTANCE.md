# 统一后台五角色验收台账

> 本台账只记录已实施的证据；不以页面数量、菜单数量或 mock 假成功替代验收。

完整 1333 项平台后台操作、18 项客服操作及五角色范围定义见 [统一后台功能总索引](./ADMIN-PLATFORM-FUNCTION-INDEX.md)。

## 关闭标准

每个 `docs/generated/features-master.tsv` 操作必须同时具备：真实 Vben 页面、真实 API、后台 JWT/RBAC/数据范围拒绝测试、中文模拟数据回归、所涉状态机或幂等测试、同状态截图。缺少任一项即保持“进行中”。

## 2026-08-03：店铺分组

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| CRMEB 店铺分组操作 | 已实现（代码） | `GET/POST/PUT/DELETE /store-groups`、详情、状态级联、模板绑定和关联店铺列表覆盖 `systemStoreGroup*` 基线语义。 |
| RBAC 与角色边界 | 已通过本地 MySQL 运行时验收 | `TestStoreGroupHTTPRBACAcrossFiveRoles` 串联生产同构 JWT、会话版本、默认角色/区域限制及按钮权限链；平台读取为 HTTP 200，商户、区域、运营、客服均为 HTTP 403，未登录为 HTTP 401。 |
| 树与关联约束 | 已通过本地 MySQL 运行时验收 | 使用 `qixi_crm_a_store_group`、`qixi_crm_a_store_group_merchant`；隔离库中文三级树实测拒绝根分组移动至孙分组，关联不存在的统一后台商户投影请求原子拒绝。 |
| 模板与状态 | 已通过本地 MySQL 运行时验收 | 非零模板 ID 必须存在于平台 DIY 页；缺失模板为 HTTP 400、缺失分组为 HTTP 404；实测停用根分组按路径同步停用子、孙分组。 |
| 中文模拟数据 | 已提供 | `sql/admin/05_test_data.sql` 含“七禧中文演示商圈”“华东精选店铺”和虚构店铺关联；不含真实身份或凭据。 |
| 自动化与构建 | 通过 | `api-platform go test ./...`、`admin-platform pnpm run build:platform`、`git diff --check` 通过。 |
| 隔离数据库/浏览器闭环 | 已关闭 | 临时 API 与真实 Vben 使用虚构平台账号完成“中文分组页面验收→已编辑→关联七禧演示店铺→查看关联→停用→删除”；模板弹框取消后浏览器仅有 Vite 调试日志，无 warning/error。所有临时账号和页面夹具均已清理。 |

## 2026-08-03：店铺类型

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 类型 CRUD、详情与备注 | 已通过本地 MySQL 运行时验收 | `merchant-types` 覆盖列表、详情、新增、编辑、备注、启停、删除；虚构中文类型完成创建、编辑、删除闭环。 |
| 保证金规则与授权菜单 | 已通过本地 MySQL 运行时验收 | `qixi_crm_a_merchant_type` 保存说明和保证金，`qixi_crm_a_merchant_type_menu` 保存去重后的店铺菜单代码；启用保证金时金额为零被拒绝，删除类型后菜单关联同步清理。 |
| RBAC 与中文夹具 | 已通过本地 MySQL 运行时验收 | `TestMerchantTypeHTTPRBACAcrossFiveRoles` 使用生产同构 JWT、会话版本、角色/区域限制及按钮权限链；平台读取为 HTTP 200，商户、区域、运营、客服均为 HTTP 403，未登录为 HTTP 401。夹具使用“七禧基础演示店”等虚构中文数据。 |
| 自动化与构建 | 通过 | `api-platform go test ./...`、`admin-platform pnpm run build:platform`、`git diff --check` 通过。 |
| 隔离数据库/浏览器闭环 | 已关闭 | 临时 API 与真实 Vben 使用虚构平台账号完成“中文类型页面验收→已编辑→备注→停用→删除”；创建时关联菜单且验证 ¥199.50 保证金。浏览器应用控制台仅有 Vite 调试日志，无 warning/error；临时账号和夹具已清理。 |

## 2026-08-03：店铺保证金与分账申请

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 保证金账户与流水 | 已通过本地 MySQL 运行时验收 | `qixi_crm_a_merchant_deposit_account`、`ledger`、`refund` 分离余额、不可变流水和退款状态；退款打款和扣减均验证余额 `500→400`、版本 `1→2`，且各自只产生一条不可变流水。 |
| 扣减与退款幂等、资金一致性 | 已通过本地 MySQL 运行时验收 | 扣减、打款均将幂等键绑定金额/中文原因或打款凭证。相同命令可重放且不重复扣款；同键改金额、原因、凭证，或跨退款复用键均返回冲突。金额限两位小数，分以下金额返回 400；余额不足的扣减或打款登记为 409 且余额/版本/流水不变；同事务更新状态、余额/版本与流水。 |
| 退款审核并发与审计 | 已通过本地 MySQL 运行时验收 | 审核说明必填且限 500 字；六个“通过/驳回”并发请求仅一条状态转换成功、五条冲突。成功审核在同一事务写入一条 `refund_approved` 或 `refund_rejected` 不可变流水，余额不变；通过时账户进入 `refund_pending`。 |
| 分账申请审核 | 已通过本地 MySQL 运行时验收 | `merchant_profitsharing_application` 与不可变 `audit` 记录申请列表、详情、审核和备注；`TestProfitsharingReviewIntegrationConcurrencyAndAudit` 验证 6 个并发审核请求恰有 1 次成功、5 次冲突，审核审计仅一条且保留原始中文审核说明。 |
| 五角色 HTTP/RBAC | 已通过本地 MySQL 运行时验收 | `TestMerchantFinanceHTTPRBACAcrossFiveRoles` 使用临时签发的后台 JWT 串联生产同构的 JWT、会话版本、默认角色/区域限制和按钮权限链。平台对保证金与分账申请列表均为 200；商户、区域、运营、客服均为 403；未登录为 401。 |
| Vben 页面与中文交互 | 已通过本地运行时验收 | 使用虚构“本地商户财务验收平台主管”登录真实 Vben，`/merchant/deposits` 显示隔离库保证金余额与中文监管说明，`/merchant/applyments` 显示虚构分账申请。扣减 ¥0.001 被前置拦截为“精确到分”，空审核说明被前置拦截；取消弹框不再产生 Vue 控制台错误。页面不展示账户或渠道密钥。 |
| 中文夹具与静态回归 | 通过 | 运行时夹具为“中文模拟退款：首笔打款登记”等虚构数据，测试后自动清理；`api-platform go test ./...`、`admin-platform pnpm run build:platform`、`git diff --check` 通过。 |
| 隔离数据库/浏览器闭环 | 部分关闭 | 打款登记、余额不足拒绝、退款审核竞争、分账申请审核、五角色 HTTP/RBAC 与两张 Vben 页面截图验收已完成。保证金账户是统一后台资金域事实表，并无商户库来源投影；剩余边界是商户入驻 NATS 端到端投递与 C 端申请回写联调，不能误称为“保证金三库投影”。 |

## 2026-08-03：统一后台菜单真实页面防线

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 未实现页面处理 | 已实现 | `admin-platform/src/utils/ecrm-menu.ts` 过滤没有 `registry.ts` 真实组件的服务端叶子，目录在子页均被过滤后同步隐藏；不再注册 `ecrm/placeholder/index`。 |
| 种子路由约束 | 已实现 | `api-platform/internal/domain/business_sql_fixture_contract_test.go` 逐项校验 `sql/admin/02_data.sql` 中每一个 `page` 路由均存在 Vben 组件映射。 |
| 自动化与构建 | 通过 | `api-platform go test ./internal/domain`、`admin-platform pnpm run build:platform`、`git diff --check`。 |
| 全量完成度 | 未关闭 | 当前受保护的是已迁移的种子菜单，不能替代 CRMEB 1333 项平台操作、18 项客服操作的逐项 API、范围、中文数据、截图与运行时闭环。 |

## 2026-08-04：客服人员名册与客服设置

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 客服人员管理 | 已实现（代码） | 统一后台“管理员管理”已负责客服账号的新建、编辑、启停、密码重置和 `service_queue` 范围配置；客服工作台新增“客服人员管理”入口与 `GET /customer-service/agents` 安全名册。 |
| 转接目标范围 | 已实现（代码） | 名册仅返回 `customer_service` 角色的最小投影；客服仅看与自身队列共享店铺的坐席，平台可监管全量。转接接口仍逐请求校验目标坐席启用状态和目标店铺范围。 |
| 客服设置 | 已实现（代码） | `GET/PUT /customer-service/settings` 保存到 `qixi_crm_a_config.customer_service.settings`；仅平台角色可写，字段仅含分配策略、并发上限和自动回复业务文案。 |
| 凭据边界 | 已实现 | 客服设置、名册和返回值均不包含密码、手机号、IM Token、UserSig 或云密钥；IM 传输继续由 pte-live-im 管理。 |
| 中文模拟数据与测试 | 已通过静态回归 | `sql/admin/05_test_data.sql` 使用“虚构演示客服将在工作时间内回复您”配置；客服处理器单元测试覆盖中文修剪、自动回复、并发上限与共享店铺边界。 |
| 构建 | 通过 | `go test ./internal/admin/customerservice ./internal/pkg/middleware ./cmd`、`admin-platform pnpm run build:platform`、`git diff --check` 通过。 |
| 隔离数据库/浏览器闭环 | 部分关闭 | `TestCustomerServiceHTTPRBACAndQueueClosure` 已在隔离 admin/business MySQL 使用虚构中文账号、店铺和会话运行生产同构 JWT、会话与路由链：平台可更新设置；商户、区域、运营、客服写设置均为 403。客服人员名册页面截图仍待补。 |

## 2026-08-04：客服人员服务用户与会话记录

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 客服服务用户 | 已实现（代码） | `GET /customer-service/agents/:agent_id/users` 从权威的客服分配绑定读取“客服的全部用户/聊天用户列表”；当前工作台可从客服人员名册进入该列表。 |
| 数据范围与隐私 | 已实现（代码） | 非平台账号仅查询自身 `service_queue.store_ids` 内的会话；列表中手机号脱敏，订单与售后详情仍必须通过既有会话范围校验。 |
| 会话记录入口 | 已实现（代码） | 服务用户可进入同一会话的订单/系统事件与转接审计；IM 聊天正文、UserSig、WebSocket 不在本服务伪造，继续由 pte-live-im 管理。 |
| 自动化与构建 | 通过 | 客服处理器测试覆盖脱敏规则与既有范围校验；`go test ./internal/admin/customerservice ./internal/admin/auth ./internal/pkg/middleware ./cmd`、`admin-platform pnpm run build:platform` 通过。 |
| 隔离数据库/浏览器闭环 | 部分关闭 | 隔离双库已以两名客服、两家中文店铺和范围外会话实测：客服列表只含授权店铺会话，范围外详情为 404，范围内会话可领取、转交并写中文用户备注；测试夹具已清理。真实 Vben 平台账号已显示“七禧客服页面验收店”与中文会话，并在“事件”抽屉显示订单/系统事件和“不保存聊天正文”边界。截图文件未能从浏览器临时沙盒导出，名册脱敏与可持久复核截图仍待补。 |

## 2026-08-04：统一后台管理员逻辑删除

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 删除语义 | 已实现（代码） | `DELETE /setting/admins/:id` 将账号逻辑删除并停用，同时递增认证与数据范围版本；历史审核、资金和客服转接记录保留原操作人 ID。 |
| 会话与名册收口 | 已实现（代码） | 已删除账号不能登录或通过逐请求会话校验；管理员列表、客服人员名册、客服转接目标校验均排除 `deleted_at` 非空账号。 |
| 平台保护 | 已实现（代码） | 拒绝删除当前登录账号，且拒绝删除最后一个启用的平台管理员。Vben 管理员列表提供明确的逻辑删除确认。 |
| 自动化与构建 | 通过 | `TestCustomerServiceHTTPRBACAndQueueClosure` 已串联管理员逻辑删除；`go test ./internal/admin/auth ./internal/admin/customerservice ./internal/pkg/middleware ./cmd` 通过。 |
| 隔离数据库/浏览器闭环 | 部分关闭 | 隔离 admin/business MySQL 以五个中文角色和三名虚构客服运行真实路由：商户、区域、运营、客服删除账号均为 403；平台删除当前账号为 409，删除虚构客服后 `status=0`、认证与范围版本均递增、旧令牌为 401、客服名册不再出现该账号、该账号不能作为转接目标（400）。历史转接审计保留，平台可将遗留会话转给仍启用客服；测试清理后账号、范围、会话、审计均为 0。Vben 截图仍待补。 |

## 2026-08-03：客服队列数据范围

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 统一后台账号配置 | 已实现 | `POST/PUT /api/platform/v1/setting/admins` 对 `customer_service` 强制校验并保存 `service_store_ids`，写入 `qixi_crm_a_data_scope.service_queue`。 |
| 服务端角色边界 | 已实现 | 系统设置账号、角色、菜单接口仅允许 `platform` 角色，拒绝仅有 `operations` 的后台令牌。 |
| 客服会话范围 | 已实现 | `GET/DETAIL/CLAIM /customer-service/threads` 对客服读取 `service_queue.store_ids`；平台角色保留全量监管范围。 |
| 并发领取 | 已实现（代码） | 领取会话在业务库事务中对绑定记录使用 `FOR UPDATE`，并以“未领取或本人已领取”的条件更新保护；其他客服不能覆盖已有归属，同一客服重复领取可安全重试。 |
| 中文模拟数据 | 已通过单元测试 | 模拟账号 `cs_demo_zh`、姓名“客服张敏”、授权店铺 `1001,1002`；无真实姓名、手机号、密码或第三方凭据。 |
| 后端自动化 | 通过 | `go test ./internal/admin/auth ./internal/admin/customerservice ./internal/pkg/middleware`，覆盖客服领取归属冲突、中文快捷回复和队列范围。 |
| 前端静态检查 | 未关闭 | 全量 `vue-tsc` 被仓库既有类型错误阻断；本次涉及文件未出现类型错误。 |
| 数据库/浏览器联调与截图 | 部分关闭 | `TestCustomerServiceHTTPRBACAndQueueClosure` 已在隔离 admin/business MySQL 验证平台列表 200，商户/区域/运营列表 403，客服列表仅返回授权店铺；范围外详情 404，范围外快捷回复写入 403。真实 Vben 以虚构平台账号显示中文店铺、会话、IM SDK 标识及“订单辅助/事件/领取”操作；事件抽屉显示系统事件且明确不返回聊天正文。浏览器临时沙盒截图文件未能导出，故不把本项标记为截图验收完成；临时库与账号已清理。 |

## 2026-08-03：客服会话转接与可追溯审计

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 转接授权与范围 | 已实现（代码） | `POST /customer-service/threads/:id/transfer` 在业务库事务中锁定会话；仅平台或当前领取客服可转接，目标后台用户须启用、具有 `customer_service` 角色并拥有相同店铺 `service_queue` 范围。 |
| 幂等与并发 | 已实现（代码） | 每次请求要求 `X-Idempotency-Key`；`(binding_id,idempotency_key)` 唯一约束、行锁与条件更新确保同键重试不重复写审计，其他人不能并发覆盖会话归属。 |
| 不可变审计 | 已实现（代码） | `qixi_crm_b_customer_service_assignment_log` 单独保存原客服、目标客服、操作人、中文原因与时间；接口不返回幂等键。客服工作台“会话记录 → 转接审计”可按已授权会话查看。 |
| 中文模拟数据 | 已提供 | 业务夹具以虚构坐席 `9301/9302` 与“虚构演示：用户咨询居家商品，转交对应队列客服。”验证 utf8mb4 中文转接原因；不含 IM 正文、真实身份或凭据。 |
| 自动化与构建 | 通过 | 客服处理器的领取并发与转接参数测试、业务 SQL/中文夹具契约、`pnpm run build:platform`、`git diff --check` 通过。 |
| 本地数据库/浏览器闭环 | 部分关闭 | 隔离 admin/business 双库已使用平台、商户、区域、运营及三名同范围客服的生产同构身份执行真实路由：客服领取后转接至同范围目标客服，同键重放不重复写审计、同键改中文原因 409；第三名非当前领取客服转接为 403，范围外详情/用户备注为 404；转接日志仅一条。审计页 Vben 截图仍待补。 |

## 2026-08-03：客服事件投影

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 统一后台事件读取 | 已实现 | `GET /customer-service/threads/:id/messages` 先按授权店铺校验会话，再读取订单/系统事件投影。 |
| Vben 入口 | 已实现 | 客服工作台新增“事件”抽屉，展示事件、来源、时间与对应 IM 会话 ID。 |
| IM 边界 | 已保持 | 响应明确标注仅为订单/系统事件投影；聊天正文、UserSig、WebSocket 继续只由 `pte-live-im` 提供。 |
| 自动化与构建 | 通过 | 客服处理器测试与 `pnpm run build:platform` 通过。 |
| 完整 18 项客服迁移 | 未关闭 | 验证码、扫码登录、图片上传等旧路由及其统一后台等价能力仍需逐项对照、补齐数据范围和同状态截图；已迁移的订单/退款/物流辅助、用户备注不可替代全量验收。 |

## 2026-08-03：客服订单与售后辅助

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 关联订单 | 已实现 | `GET /customer-service/threads/:id/order` 必须先通过会话的授权店铺校验，订单再以 `store_id` 二次约束。 |
| 关联退款 | 已实现 | `GET /customer-service/threads/:id/refunds` 通过会话关联订单和店铺内连接读取，不接受任意退款 ID。 |
| 用户资料 | 已实现 | `GET /customer-service/threads/:id/user` 仅以授权会话 `user_id` 查询基础资料，客服不能构造任意用户 ID。 |
| Vben 入口 | 已实现 | 客服工作台新增“订单辅助”弹窗，展示关联订单、退款单和用户资料。 |
| 自动化与构建 | 通过 | 客服处理器测试、`pnpm run build:platform`、`git diff --check` 通过。 |

## 2026-08-03：客服快捷回复

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 统一后台入口 | 已实现 | 客服工作台新增“快捷回复管理”，包含按店铺查询、新增、编辑、启停与删除。 |
| 业务数据归属 | 已实现 | 新增 `qixi_crm_b_customer_service_quick_reply`，不复用旧独立客服链路及 `qixi_m_admin_*` 表。 |
| 数据范围与审计 | 已实现 | 所有读写均按 `service_queue.store_ids` 过滤；范围外店铺和回复 ID 不可读写。删除仅写 `deleted_at`，并记录创建/更新后台用户 ID。 |
| 中文模拟数据 | 已提供 | `sql/business/05_test_data.sql` 提供“七禧演示茶铺”“发货时效”等明显虚构中文夹具，不含真实个人信息或凭据。 |
| 自动化与构建 | 通过 | 客服处理器单元测试覆盖中文内容、状态校验和店铺范围；`pnpm run build:platform`、`git diff --check` 通过。 |
| 本地数据库/浏览器闭环 | 部分关闭 | `TestCustomerServiceHTTPRBACAndQueueClosure` 在隔离业务库中以 UTF-8 虚构店铺和客服账号实际完成“授权店铺新增 → 列表可见 → 编辑为停用 → 软删除 → 列表隐藏”；范围外店铺的创建与查询为 403、更新为 404。库内复核被删除记录保留 `disabled` 状态、操作者及 `deleted_at`，测试后客服、快捷回复均清理为 0。Vben 同状态截图仍待补。 |

## 2026-08-03：客服用户备注

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 统一后台入口 | 已实现 | 会话“订单辅助”面板增加用户备注读取与保存。 |
| 受控关联 | 已实现 | `PUT /customer-service/threads/:id/user-note` 先校验会话及授权店铺，再从会话推导 `user_id`、`store_id`；接口不接受任意用户 ID。 |
| 并发与审计 | 已实现 | `qixi_crm_b_customer_service_user_note` 以 `(user_id,store_id)` 唯一键执行 upsert，记录 `updated_by` 与更新时间。 |
| 中文模拟数据 | 已提供 | 业务库夹具含“已告知七禧演示茶铺的售后处理时效”等虚构备注。 |
| 自动化与构建 | 通过 | 客服处理器测试覆盖中文修剪、500 字限制；`pnpm run build:platform`、`git diff --check` 通过。 |
| 本地数据库/浏览器闭环 | 部分关闭 | `TestCustomerServiceHTTPRBACAndQueueClosure` 使用同一虚构用户在两家中文店铺的两个会话：授权客服写店铺 A 备注成功，访问店铺 B 会话为 404；平台写入店铺 B 的独立中文备注后，直接查询确认两行按 `(user_id, store_id)` 共存且互不覆盖。夹具在测试结束后清理为 0；Vben 同状态截图仍待补。 |

## 2026-08-03：客服订单配送与商品辅助

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 订单配送 | 已实现 | `GET /customer-service/threads/:id/delivery` 先校验授权会话，再以会话店铺关联 `qixi_crm_b_order_delivery` 与订单；无物流如实返回不存在。 |
| 关联商品 | 已实现 | `GET /customer-service/threads/:id/products` 仅返回该会话订单的下单商品快照，并以会话店铺关联当前商品投影；不能提交任意商品 ID 查询。 |
| Vben 入口 | 已实现 | “订单辅助”面板新增配送方式、承运商、运单号、配送状态及下单商品/当前库存展示。 |
| 中文模拟数据 | 已提供 | 业务库夹具提供 `CS-DEMO-*` 虚构订单、针织衫商品、演示快递、退款和 IM 会话 ID。 |
| 自动化与构建 | 通过 | 客服处理器测试、`pnpm run build:platform`、`git diff --check` 通过。 |
| 本地数据库/浏览器闭环 | 未关闭 | 需启动本地业务库，验证授权店铺可见、其他店铺会话及订单 ID 无法横向读取，并保存页面截图。 |

## 2026-08-03：商户监管数据范围

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 统一后台账号配置 | 已实现 | `merchant` 角色必须配置 `merchant_ids`，保存为 `qixi_crm_a_data_scope.merchant`。 |
| 商户监管接口 | 已实现 | `/merchants` 列表、详情、启停按授权商户 ID 或区域 ID 服务端过滤；无范围时返回 403，不降级为全量。 |
| 区域角色 | 已实现 | 仅对已实现范围过滤的 `/merchants` 接口开放；其他未迁移区域接口继续由中间件拒绝。 |
| 中文模拟数据 | 已通过单元测试 | 模拟账号 `merchant_demo_zh`、姓名“商户运营李航”、授权商户 `2001,2002`；无真实个人信息或凭据。 |
| 后端自动化 | 通过 | 商户范围解析及账号配置测试通过。 |
| Vben 构建 | 通过 | `pnpm run build:platform`。 |

## 2026-08-03：区域入驻申请审核范围

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 平台分配区域 | 已实现 | Vben 入驻审核页新增“分配区域”；`POST /merchant-intentions/:id/assign-region` 仅允许 `platform`，且仅允许待审核申请。 |
| 区域读取范围 | 已实现 | 申请列表、详情在 SQL 层以 `region_id IN (region_ids)` 过滤；未配置区域范围、商户监管角色均返回 403。 |
| 区域审核范围 | 已实现 | 审核事务再次带入区域过滤；区域角色只能以已分配的相同区域开通，不能篡改 `region_id`。 |
| 中间件开放 | 已实现 | `RestrictRegionConsole` 仅为已实现服务端过滤的 `/merchant-intentions` 放行。 |
| 自动化与 Vben 构建 | 通过 | `api-platform go test ./...`、`pnpm run build:platform`、`git diff --check`。 |
| 三库/NATS 中文数据闭环 | 未关闭 | 需本地环境以“七禧演示茶铺”“区域主管王小明”等虚构数据验证分配前不可见、分配后可见、范围外详情/审核拒绝，以及开店重试。 |

## 2026-08-03：商户入驻开店命令

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 审核通过的写入边界 | 已实现 | 平台审核不再写入 `qixi_crm_m_*`；改由 NATS request/reply 发送受控开店命令。 |
| 店铺创建事务 | 已实现 | `api-merchant` 在本库事务内创建商户、店铺和 owner 账号；平台仅回写 `qixi_crm_a_merchant_view` 与申请审核投影。 |
| 幂等与敏感数据 | 已通过隔离 MySQL 并发验收 | 以申请 ID 构造确定性 `app_id`；8 个并发中文开店命令收敛为 1 个商户、1 个店铺、1 个 owner 账号，均返回同一店铺结果。平台审核投影以 `status='pending'` 条件更新，6 个并发收口恰有 1 次成功、5 次“已审核”拒绝；命令只传 bcrypt 哈希，不写入或记录密码明文。 |
| 参数约束 | 已实现 | 通过审核必须指定区域、管理账号及至少 8 位初始密码；移除了历史默认密码。 |
| 后端自动化 | 通过 | `TestApplyIntegrationConcurrentChineseOnboardingIsIdempotent` 与 `TestFinalizeIntentionApprovalIntegrationConcurrentProjection` 在隔离 MySQL 通过；对应模块单测与差异检查通过。 |
| NATS request/reply 与 C 端申请回写 | 部分关闭 | 临时 NATS 已运行真实商户订阅器，UTF-8 中文命令在 request/reply 中创建店铺并在重投时返回同一结果；平台客户端也已校验相同 subject、中文载荷与响应契约。仍需使用完整平台审核处理器和 C 端申请 outbox 执行“C 端提交→后台投影→审核→开店→重投”，并保存五角色和页面截图。 |

## 2026-08-03：区域/商户订单监管范围

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 统一范围解析 | 已实现 | 新增 `internal/adminscope`，从 `qixi_crm_a_data_scope` 解析 `platform`、`merchant_ids`、`region_ids`。 |
| 订单列表与详情 | 已实现 | `/orders` 与 `/orders/:id` 先解析统一范围，再按商户 ID 查询；未配置范围返回 403。 |
| 中文模拟数据 | 已通过单元测试 | 模拟“商户运营李航”拥有商户 `2001,2002` 和区域 `10,20`，验证直接商户范围与区域范围不混淆。 |
| 数据库联调与截图 | 通过（隔离三库 HTTP；Vben 平台页） | `TestOrderHTTPRBACAndRegionalScope` 在隔离 admin/business/merchant 三库通过真实统一后台 JWT、会话、角色总闸和区域映射验收：平台可见区域 10/20 两笔虚构中文订单；商户直管范围和区域 `{10}` 都只可见区域 10，区域 20 详情为 404；运营、客服及未配置范围的商户均为 403。实测 `paid=0/1` 筛选、授权订单的中文商品明细和虚构物流快照；路由只注册 GET，POST 为 404，未把履约或支付写入平台监管接口。另以一次性假凭据 Vben 环境打开“订单监管”，页面显示“七禧控制台茶铺/花店”的两笔 UTF-8 虚构订单、已支付、待发货/待收货、¥100/¥50 与两个详情按钮；页面明确标注只读，浏览器控制台无 error。测试夹具与页面临时库均在结束后清理。 |

## 2026-08-03：区域/商户商品监管范围

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 商品读取范围 | 已实现 | `/products`、`/products/:id` 通过 `adminscope.ResolveMerchantScope` 解析：平台全量、商户按 `merchant_ids`、区域按 `region_ids → qixi_crm_m_merchant.region_id`；无范围返回 403。 |
| 越权详情保护 | 已实现 | 详情与列表复用同一 `merchant_id IN (...)` SQL 约束，范围外商品返回不存在，不能靠商品 ID 横向读取。 |
| 审核按钮 | 已迁移至统一 RBAC | 审核仅限 `platform`，并以新 `qixi_crm_a_menu / role_menu / admin_user_role` 中 `product.audit.submit` 按钮码校验；不再依赖旧系统菜单表。 |
| 分类、品牌维护 | 已迁移至统一 RBAC | 全局分类和品牌仅 `platform` 可读取；写操作分别校验 `product.category.manage`、`product.brand.manage` 按钮码。商户、区域角色不再分配这两个菜单，避免出现无数据范围的全局配置入口。 |
| Vben 页面 | 已实现 | 商品页按统一后台账号角色和按钮码展示审核操作；商户/区域角色只读并显示“授权范围”语义。 |
| 中文模拟数据 | 已提供 | 商户库夹具提供“七禧演示茶铺”“七禧居家优选店”“七禧数码生活店”，区域 10/20 与在售、待审、草稿商品。 |
| 自动化与构建 | 通过 | 商品范围合并、状态映射、角色总闸和按钮配置失败关闭回归；`pnpm run build:platform`、`git diff --check` 通过。 |
| 本地三库/浏览器闭环 | 部分关闭 | `TestProductHTTPRBACAndRegionalScope` 已以中文区域 10/20 的两家虚构商户、两家店铺和两件待审商品运行生产同构统一后台路由。平台列表可见两件；商户直管范围与区域 10 均只见本范围商品，范围外详情为 404；运营、客服列表为 403，商户/区域/运营/客服审核均为 403。审核状态与 `qixi_crm_m_product_audit_outbox` 在商户库同事务提交；平台审核事实和业务商品/SKU 视图按 source event 幂等投影，投影失败后由租约重投恢复。隔离库重复执行建表脚本及重投测试均通过，夹具清理后五类数据计数均为 0。Vben 同状态截图仍待补。 |

## 2026-08-03：非平台首页数据范围

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 服务端范围汇总 | 已实现 | `/dashboard/summary` 平台账号读取全量；商户/区域由 `merchant_ids`、`region_ids` 反查店铺；客服读取 `service_queue.store_ids`。未配置范围返回 403。 |
| 跨店数据保护 | 已实现 | 店铺、商品、订单、配送、客服、浏览、访客、销售排行均附带 `store_id IN (...)`；退款通过关联订单过滤，不能由退款 ID 的间接汇总泄露其他店铺。 |
| 语义一致性 | 已实现 | 受限范围不能安全推导全局注册用户，接口标记为 `scope=store`，Vben 改显示“下单用户”，避免错误地把全站新增用户当作店铺数据。 |
| 非法快捷入口 | 已收口 | 非平台首页仅显示已开放的商品/订单或客服入口，不再展示会被默认拒绝策略拦截的营销、系统设置等入口。 |
| 服务端菜单一致性 | 已收口 | 初始化 SQL 会撤销商户的未迁移营销/内容、区域的未迁移区域管理、运营的首页/用户菜单；对既有库也执行撤销，避免 `INSERT IGNORE` 留下历史假入口。 |
| 自动化与构建 | 通过 | 覆盖范围 ID 去重与角色识别；`cd api-platform && go test ./...`、`cd admin-platform && pnpm run build:platform`、`git diff --check` 通过。 |
| 本地三库/浏览器闭环 | 通过（隔离三库 HTTP；Vben 平台页） | `TestDashboardHTTPFiveRoleScopeAndRank` 使用区域 10/20 的两家中文商户/店铺、订单、退款、会话、浏览、关注及审核夹具，运行真实统一后台 JWT、会话、角色总闸与三库映射。平台统计两家店铺；商户直管、区域 `{10}` 与客服店铺 `{1}` 都只返回店铺 1 的统计、待办、浏览/访客、退款和排行，运营与无范围商户均为 403。排行将关注数预聚合后再关联订单，2 名关注用户不会把店铺 A 的单笔 ¥100、销量 2 错算为 ¥200、销量 4。另以一次性假凭据 Vben 平台账号打开控制台：新增用户/浏览量/访客数均为 3、店铺数为 2，中文店铺排行显示关注 2、销量 2、¥100.00 与关注 1、销量 1、¥50.00；服务端统计测试已验证该视图对应全量 `scope=all`。浏览器控制台无 error，三库测试夹具与页面临时库均在结束后清理。 |

## 2026-08-03：区域/商户售后监管范围

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 售后列表、详情、审核与驳回 | 已接入统一范围 | `/refunds` 全部读取与状态流转先解析 `qixi_crm_a_data_scope`，再按可监管商户过滤。 |
| 未授权处理 | 已实现 | 未配置范围返回 403；范围外售后不返回详情且不能变更状态。 |
| 状态机 | 保持 | 审核/驳回仍使用行锁、条件更新与幂等事件键；实际支付退款回调不由此接口伪造。 |
| 集成验收 | 部分关闭（隔离三库 HTTP） | `TestRefundHTTPRBACAndMerchantRegionScopes` 使用生产同构 JWT、会话版本、角色关系和真实注册路由：平台可读取区域内外两笔虚构中文退款；商户与区域仅可读取“区域内”退款，范围外详情为 404；运营、客服及未配置范围的商户账号为 403；商户和区域审批均为 403。区域退款路由已加入只读范围白名单，数据仍由处理器映射 `region_id → merchant_id` 后过滤。临时夹具自动清理。 |

## 2026-08-03：退货退款寄回与收货确认

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 退货退款状态机 | 已实现（代码） | `qixi_crm_b_refund` 增加 `refund_type` 与 `awaiting_return/awaiting_receipt`；仅退款同意后进入 `refunding`，退货退款依次进入待退货、待收货、退款处理中。 |
| 用户与商户闭环 | 已实现（代码） | 用户 `POST /refunds/:id/return-shipment` 仅可在待退货登记一次物流；商户 `POST /refunds/:id/confirm-return` 仅可在待收货确认。两端均按用户/店铺边界锁定退款单。 |
| 平台、区域与 Vben | 已实现（代码） | 退款监管按类型受理并保留区域数据范围；平台和商户 Vben 展示待退货、待收货、物流信息，商户待收货单显示“确认收货”。 |
| 资金安全 | 已实现（代码） | 确认收货只推进至 `refunding`；`refunded` 仍仅由已验证支付回调写入，未接入真实渠道或凭据。 |
| 中文夹具与自动化 | 通过（隔离 HTTP） | `TestReturnShipmentHTTPIntegration` 以生产同构 C 端 JWT 与会话验证 UTF-8 中文物流、精确重放、篡改拒绝和跨买家 404；`TestConfirmReturnHTTPIntegration` 以生产同构店铺 JWT、`X-AppId`、会话和按钮权限验证确认收货、重放、跨店 404 与单笔退款交易/事件。 |
| 同次运行时链路 | 已关闭（隔离双服务 HTTP） | 以同一虚构退款单依次调用用户寄件、商户确认收货、sandbox `refund/mock` 回调及回调重放，四个请求均为 HTTP 200；最终退款/订单/主单/支付为 `refunded/cancelled/refunded/refunded`，物流、退款交易、回调、三条流转事件、库存与结算 outbox 各一条。临时数据库、服务与令牌均已清理。 |
| 生产验收边界 | 仍为部分 | 本链路已在假凭据环境运行验证；微信签名/AES-GCM 仍需受控非真实证书夹具，且五角色退款页面截图未完整覆盖，不得据此宣称全量生产验收完成。 |

## 2026-08-03：退款可信回调与失败恢复

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 退款请求边界 | 已实现（代码） | 审核进入 `refunding` 时同一事务创建 `qixi_crm_b_refund_transaction`；只有后台执行器以服务端签名请求微信退款，审核操作和浏览器均不能写入 `refunded`。 |
| 可信终态 | 已实现（代码） | `POST /api/callback/v1/refund/wechat` 同时核验微信 V3 签名、AES-GCM 资源、商户号、内部退款单、退款金额和原交易金额；事件 ID 唯一落入 `qixi_crm_b_refund_callback` 后才推进资金终态。 |
| 失败恢复 | 已实现（代码） | 退款交易记录 `updated_at`；`created/failed` 可重试，`processing` 仅在 15 分钟无终态回调后以相同内部退款单号重试，防止进程中断永久挂起且不生成重复退款号。 |
| 自动化 | 通过 | `api-business go test ./internal/business/refundprocessor ./internal/business/order ./internal/pkg/wechatpayv3` 覆盖金额换算、超时恢复、签名请求与验签回调。 |
| 库存、结算副作用 | 已实现（代码） | 下单库存 Saga 已在业务 outbox 与商户库存账本闭环；确认收货在同一业务事务写入订单级结算应计命令，可信退款回调写入退款冲销命令。商户服务仅通过 NATS 命令落 `qixi_crm_m_settlement_entry`，只累计未冻结当期账单；冻结账期不回写，退款形成当期负向调整。 |
| 运行时验收 | 部分关闭（隔离三库 NATS） | 仅在显式 sandbox 注册的 `POST /api/callback/v1/refund/mock` 以虚构退款 ID 完成真实业务回调：服务端从锁定订单/售后项推导 ¥66.50、店铺、商户、SKU 与数量，推进 `refunding → refunded`、订单/主单/支付终态，写入库存回补与结算冲销 outbox；商户 NATS 消费后库存 `9→10`、结算账本 `-¥66.50`，平台投影显示当期负向账单。相同回调重放后回调、事件、库存命令、结算命令和商户账本均仍只有一条。未接入或伪造真实渠道凭据。 |
| 仍未关闭的生产边界 | 未关闭 | 微信签名/AES-GCM 终态回调仍需以独立的受控非真实证书夹具验证；退货寄回→商户确认→退款回调同次链路已完成，但退款监管五角色 Vben 截图尚未完整覆盖，不能据此宣称退款域整体生产验收完成。 |

## 2026-08-03：订单 SKU 身份与库存预留前置条件

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| SKU 消费投影 | 已实现（代码） | 新增 `qixi_crm_b_product_sku_view`，保存商户 SKU 主键、规格快照、售价和可售库存；C 端购物车只读取业务库投影，不跨库读取 `qixi_crm_m_product_sku`。 |
| 下单不可变事实 | 已实现（代码） | `qixi_crm_b_order_item.merchant_sku_id` 固化商户 SKU 主键；既有订单以 `0` 明确表示历史未映射，不参与新库存闭环推断。 |
| 购物车与商品审核 | 已实现（代码） | 加购、改数量、再次购买均按规格投影校验库存；平台审核上架要求存在可售 SKU，并将规格投影同步到业务库。 |
| 中文模拟数据 | 已提供 | 商户/业务夹具用 `61001` 至 `61006` 的虚构 SKU 和“默认/标准”中文规格，未使用真实用户、商户资料或凭据。 |
| 自动化 | 通过 | 购物车、订单、平台商品审核和 SQL/夹具契约测试通过；SKU 身份丢失或无可售规格会被拒绝。 |
| 预留、确认、释放 | 已实现（代码） | 订单创建同事务写 `qixi_crm_b_stock_command_outbox`；业务投递器通过 NATS 发送命令，商户侧以 SKU/预留行锁、库存条件更新与库存账本执行。支付仅在全部预留已接受后推进，并写确认命令；取消/超时关闭写释放命令。 |
| 退款库存回补 | 已实现（代码） | 可信微信退款回调和 sandbox 模拟退款回调均从 `qixi_crm_b_aftersale_item → qixi_crm_b_order_item` 推导 SKU 与数量，在同一业务事务写入 `restock` 命令；不读取浏览器传参，也不直接写商户库存。 |
| 结算冲销 | 已实现（代码） | 确认收货与可信退款分别写 `accrue/reverse` 结算命令 outbox；商户库以店铺锁、幂等键和订单/退款关联写订单级账本，并自动生成当月 `bill_pending` 账期。到期账期由商户本地任务冻结，退款不回写已冻结账期。 |
| 结算审核与打款跨服务验收 | 已关闭（局部本地闭环） | 本地临时 NATS、商户库、管理库和两个 API 进程以“七禧结算闭环验收茶铺”的虚构账单验证 `withdraw_applied → approved → paid`：平台只发送受控命令，商户服务写账单与 outbox，平台投影经 NATS 刷新。相同审核/打款键只有同动作、同操作人、同中文说明或同内部凭证才可重放；改变动作、操作人或凭证会冲突。 |
| 结算 Vben 页面验收 | 已关闭（本地假凭据） | 虚构平台账号真实打开“店铺结算监管”，对“七禧结算页面验收茶铺”的 ¥1280.50 账单完成“通过→登记本地模拟结算凭证→已打款”；页面异步刷新投影，最终只显示只读状态且浏览器无 error。页面说明明确平台不直连商户库、不发起转账、不录入收款账户或密钥。 |
| 结算订单事实链局部运行时验收 | 已关闭（隔离三库 NATS） | 真实 Gin 确认收货处理器以中文虚构订单完成 `shipped → completed` 并在同一业务事务写入 `settlement:accrue:<order_id>`；业务 outbox 经 NATS 由商户服务落不可变正/负账本和账期，再经商户 outbox 更新平台投影。账本集成测试实际调用冻结任务，验证冻结历史账单不被退款回写、退款形成当前 `bill_pending` 负向调整；同键改金额冲突。账本查重现已明确绑定表模型，所有命令键强制绑定订单/退款事实，负向账期可安全投影但商户申请提现仍要求金额大于零。 |
| 结算全链路生产验收边界 | 未关闭 | 尚未完成真实 C 端认证会话驱动的“确认收货→应计→到期冻结→申请”与可信支付渠道退款回调的同次端到端演练，也未补齐负向账期在 Vben 页面的截图、投递故障重试和五角色数据范围验收；不能据此宣称结算域整体生产验收完成。 |

## 五角色推进顺序

1. 平台：系统设置、商户审核、全局商品/订单/售后/财务监管的 RBAC 与审计。

## 2026-08-03：商户监管操作 RBAC 收口

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 店铺启停 | 已实现（代码） | `merchant.status.manage` 仅授予平台角色；服务端先校验平台角色和按钮码，商户监管/区域角色即使拿到店铺页面也只能查看状态。 |
| 入驻审核 | 已实现（代码） | `merchant.intention.audit` 授予平台与区域角色；区域审核仍受已配置 `region_ids` 限制，无法审核范围外申请。 |
| 区域分配 | 已实现（代码） | `merchant.intention.assign_region` 仅授予平台角色，页面与接口均隐藏/拒绝无权账号；避免区域角色自行扩展审核范围。 |
| 自动化与构建 | 通过 | 商户处理器及 SQL/RBAC 静态契约测试、`api-platform go test ./...`、`admin-platform pnpm run build:platform` 通过。 |
| 生产验收边界 | 未关闭 | 尚未使用隔离中文夹具分别登录五种角色，验证菜单、按钮、数据范围拒绝与审计记录截图；不得以静态构建替代运行时授权验收。 |

## 2026-08-03：商户分类与佣金规则

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 独立事实表 | 已通过本地 MySQL 运行时验收 | `qixi_crm_a_merchant_category` 保存商户分类、0–100 的平台佣金比例和状态；不复用 `qixi_crm_a_platform_category` 商品分类表。中文分类可完整新增、编辑、删除。 |
| API 语义与精度 | 已通过本地 MySQL 运行时验收 | 新增响应返回真实分类 ID；重复中文名为 409，不存在更新/删除为 404，零 ID 与三位小数佣金率为 400；同值更新保持成功。佣金比例限制为 0–100 且精确到两位。 |
| 五角色 HTTP/RBAC | 已通过本地 MySQL 运行时验收 | 真实注册路由串联 JWT、会话版本、默认角色/区域限制和 `merchant.category.manage` 校验。平台读取为 200；商户、区域、运营、客服均为 403。 |
| Vben 页面与中文交互 | 已通过本地运行时验收 | 虚构平台账号在真实 Vben 页面完成“中文商户分类验收页面→中文商户分类验收已编辑→删除”闭环并截图，列表显示中文夹具与 9.25% 佣金；浏览器控制台无 warning/error。 |
| 自动化与构建 | 通过 | `TestMerchantCategoryIntegrationCRUDAndConflict`、`TestMerchantCategoryHTTPRBACAcrossFiveRoles`、`api-platform go test ./...`、`admin-platform pnpm run build:platform`、`git diff --check` 通过。 |

## 2026-08-03：短信配置无密钥边界

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 配置模型 | 已实现（代码） | 短信后台仅保存 `provider=stub` 的启用状态、中文签名和备注；不接受 API Key、Secret、账号、密码、回调地址等未知字段。 |
| 历史数据保护 | 已实现（代码） | 读取到格式异常或含未知字段的旧缓存时返回安全默认 stub，不向浏览器回显原始内容。真实通道凭据应位于外部密钥管理，不属于统一后台数据库。 |
| 授权 | 已实现（代码） | 保存操作需要平台角色与 `setting.sms.manage` 按钮码；无权角色页面只读且服务端拒绝写入。 |
| 自动化 | 通过 | `sms_config_test.go` 覆盖密钥字段拒绝、中文模拟 stub 规范化和旧敏感值不回显。 |
| 生产验收边界 | 未关闭 | 尚未在隔离环境验证五角色按钮拒绝截图；当前不接入、也不模拟真实短信通道。 |
2. 商户：授权商户范围的商品、订单、营销、内容监管，逐接口拒绝越权商户。
3. 区域：`region_ids` 下商户、订单、结算与审核接口；未完成隔离的接口必须持续拒绝访问。
4. 客服：会话认领/转接、订单与售后辅助、快捷回复；IM 消息和 UserSig 只经 pte-live-im。
5. 运营：内容、DIY、活动、素材、报表与运营配置的操作级权限。

## 2026-08-03：运营写操作第一批 RBAC

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 文章与文章分类 | 已加服务端角色校验 | 新增、编辑、删除仅允许 `platform` 或 `operations`；不再仅依赖 Vben 菜单隐藏。 |
| 秒杀、拼团、预售 | 已加服务端角色校验 | 三类活动的更新、删除仅允许 `platform` 或 `operations`。 |
| 平台券编辑 | 已补按钮校验 | `PUT /coupons/:id` 复用平台券写权限；此前该写操作缺少服务端权限检查。 |
| 短信配置写入 | 已收紧为平台专属 | `PUT /setting/sms` 仅允许 `platform`，运营角色不能改系统通道配置。 |
| 自动化 | 通过 | `api-platform go test ./...`、`git diff --check`。 |
| 操作级菜单、运营数据范围和中文集成数据 | 未关闭 | 运营模块仍需按 311 项营销、56 项内容、86 项装修和素材/报表逐操作补齐菜单节点、范围与浏览器证据。 |

## 2026-08-03：运营统一按钮 RBAC 第二批

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 文章与分类 | 已迁移 | 新增 `content.article.manage`、`content.article_category.manage`；文章和分类的增改删要求 `platform/operations` 角色及统一后台按钮码，并读写 `qixi_crm_a_article*`，不再依赖旧后台表。 |
| 公告、协议 | 已迁移 | 新增 `content.notice.manage`、`setting.agreement.manage`；公告读写迁至 `qixi_crm_a_notice`，协议与安全短信 stub 迁至 `qixi_crm_a_setting_cache`，不再依赖旧配置/缓存表。 |
| 装修 | 已迁移 | 新增 `operations.diy.manage`；页面、分类、链接的新增、修改、启用、复制、恢复和删除走统一后台按钮校验。 |
| 营销活动 | 已迁移 | 秒杀、拼团、预售的更新、删除分别校验 `marketing.seckill.manage`、`marketing.combination.manage`、`marketing.presell.manage`。 |
| Vben 按钮 | 部分收口 | 文章、公告、协议和装修主页面按权限码隐藏写操作；秒杀已补活动名称、场次、日期、价格、限购、启停和软删除表单，拼团/预售仍待补完整编辑表单与中文集成闭环。 |
| 自动化与构建 | 通过 | `cd api-platform && go test ./...`、`cd admin-platform && pnpm run build:platform`、`git diff --check` 通过。 |
| 生产验收 | 未关闭 | 本批接口的业务投影仍有历史表依赖；尚缺运营数据范围、中文夹具、数据库/浏览器截图和剩余运营模块逐项迁移。 |

## 2026-08-04：运营秒杀活动配置编辑

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| Vben 编辑与删除 | 已实现（代码） | 秒杀监管页通过现有 `GET/PUT/DELETE /seckill/actives/:id` 读取、维护名称、场次、日期、秒杀价、单次限购和状态；删除保持服务端软删除。 |
| 业务边界 | 已实现（代码） | 页面不允许变更商品与商户归属；服务端拒绝非 ISO 日期与结束早于开始的配置，已产生订单及其价格快照不被修改。 |
| RBAC | 已保持 | 写与删均要求 `platform|operations` 和 `marketing.seckill.manage`，Vben 仅在具有该按钮码时显示操作。 |
| 自动化与构建 | 通过 | `go test ./internal/domain/seckill ./internal/platform/seckill ./cmd`、`admin-platform pnpm run build:platform` 通过。 |
| 运行时闭环 | 未关闭 | 共享 Docker 不可作为无凭据验收库使用；需在符合仓库运行约束的隔离环境以中文活动夹具验证编辑、软删除、订单价格不变与权限截图。 |

## 2026-08-04：运营拼团活动配置编辑

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| Vben 编辑与删除 | 已实现（代码） | 拼团监管页通过现有 `GET/PUT/DELETE /combination/groups/:id` 维护拼团价、成团人数、成团时限、活动时间、展示和状态，并提供软删除入口。 |
| 业务边界 | 已实现（代码） | 页面不变更商品或商户归属；服务端拒绝非法或反向活动时间，进行中/已完成团单的成员与订单价格快照不受配置编辑影响。 |
| RBAC | 已保持 | 写与删均要求 `platform|operations` 和 `marketing.combination.manage`，Vben 仅在具备按钮码时展示操作。 |
| 自动化与构建 | 通过 | `go test ./internal/domain/combination ./internal/platform/combination ./cmd`、`admin-platform pnpm run build:platform` 通过。 |
| 运行时闭环 | 未关闭 | 需在符合仓库运行约束的隔离环境以中文拼团活动验证编辑、软删除、时间拒绝和角色截图。 |

## 2026-08-03：运营平台券统一授权

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 操作级权限 | 已迁移 | 平台券创建、更新、启停、删除均要求 `platform/operations` 角色和 `marketing.coupon.manage` 统一按钮码，不再读取旧系统菜单。 |
| 状态机边界 | 保持 | 继续使用 `promotion.Service`：状态只接受启用/停用，删除为软删除；券适用规则仍遵循普通订单可用、活动订单不参与平台券计价。 |
| Vben 页面 | 已实现 | 平台券页面按按钮码显示新增、编辑、启停与删除，不拥有该码的账号只读。 |
| 自动化与构建 | 通过 | 平台券处理器、促销领域、权限中间件测试与 Vben 构建、差异检查通过。 |
| 生产验收 | 未关闭 | 现有促销投影仍需迁离历史表，且缺中文券夹具、并发/领券扣减集成测试与浏览器截图。 |

## 2026-08-03：运营素材与社区统一授权

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 素材库 | 已迁移 | 素材分类增改删、上传、删除统一要求 `content.attachment.manage`，页面按该码隐藏管理操作；平台/运营素材已迁至 `qixi_crm_a_attachment_category` 与 `qixi_crm_a_attachment_asset`。 |
| 社区审核 | 已迁移 | 审核、展示与置顶要求 `content.community.audit`；运营可审核但不能仅凭此码删除帖子。社区帖子、话题、分类、评论及用户/商品投影统一读取 `qixi_crm_b_social_*`、`qixi_crm_b_user`、`qixi_crm_b_product_view`。 |
| 社区删除 | 已迁移 | 帖子、评论删除要求独立的 `content.community.delete`，前端按 `platform/operations` 和对应按钮码显示。 |
| 自动化与构建 | 通过 | 素材/社区处理器、权限中间件测试，Vben 构建及差异检查通过。 |
| 生产验收 | 未关闭 | 素材存储配置仍待闭环；社区已具备中文夹具并迁离历史表，仍未完成上传病毒/类型扫描、运营范围隔离和浏览器截图。 |

## 2026-08-03：用户标签与分组业务库迁移

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 标签与打标 | 已迁移 | 标签、标签关系统一改读写 `qixi_crm_b_user_label`、`qixi_crm_b_user_label_relation`；列表、维护与指定用户打标均要求 `user.label.manage`。 |
| 用户分组 | 已迁移 | 分组统一改读写 `qixi_crm_b_user_group`；列表和增改删均要求 `user.group.manage`。 |
| 中文夹具 | 已提供 | 提供“高频复购用户”“香氛兴趣用户”“七禧精选会员”等虚构中文数据，关联本地演示用户。 |
| 生产验收 | 未关闭 | 待在隔离本地运行环境执行接口闭环、验证跨角色拒绝访问并补浏览器截图。 |

## 2026-08-03：区域商圈与代理统一后台迁移

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 表与数据归属 | 已迁移 | 商圈和代理申请改读写 `qixi_crm_a_business_zone`、`qixi_crm_a_business_zone_agent`，不再访问旧 `qixi_m_admin_circle*` 表。 |
| RBAC | 已收紧 | 仅 `platform` 角色可访问；区域商圈维护、代理维护、代理审核分别要求 `region.zone.manage`、`region.agent.manage`、`region.agent.review`。 |
| 结算资料保护 | 已实现 | 结算账号、开户行与二维码仅写入、不在列表或详情回传；编辑时空值保持原资料，Vben 明确提示“不回显”。 |
| 中文夹具 | 已提供 | 提供“华东中文演示区域”“区域主管王小明”等虚构数据；结算账号字段明确为空。 |
| 生产验收 | 未关闭 | 尚待隔离本地库验证平台允许、区域/商户/运营/客服拒绝、代理审核幂等和浏览器截图。 |

## 2026-08-03：物流基础配置统一后台迁移

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 数据表 | 已迁移 | 快递公司和行政区划统一改读写 `qixi_crm_a_express`、`qixi_crm_a_city`；店铺运费模板仍保持 merchant 库事实，不由当前平台接口越库写入。 |
| RBAC | 已收紧 | 快递公司和城市查询、增改删只允许 `platform`，并要求 `freight.express.manage` 按钮码。 |
| 中文夹具 | 已提供 | 提供“七禧演示快递”“本地验收物流”“中国演示区域”等虚构中文配置，无真实承运商凭据。 |
| 生产验收 | 未关闭 | 商户店铺级运费模板编辑尚未迁入统一后台；待隔离本地库验证权限拒绝和浏览器截图。 |

## 2026-08-03：运营直播审核统一授权

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 审核权限 | 已迁移 | 直播间通过、驳回、显示、隐藏均要求 `platform/operations` 角色和 `marketing.broadcast.audit` 按钮码。 |
| Vben 页面 | 已实现 | 直播监管页使用统一按钮码决定审核操作；无该码账号仅可查看房间与挂货详情。 |
| 状态机 | 保持 | 审核状态和显示状态继续由 `broadcast.Service.Audit` 校验，本轮未修改直播供应链或状态转换。 |
| 业务表与敏感字段 | 已实现（代码） | 直播间和挂货改读 `qixi_crm_b_broadcast_room*`，商品与商户名称改取业务商品投影；后台 API 不再序列化推流地址和主播手机号。 |
| 中文模拟数据 | 已提供 | 业务夹具加入“七禧服饰秋日穿搭直播间”“七禧居家香氛新品直播间”及挂货数据，推流地址和手机号明确为空。 |
| 自动化与构建 | 通过 | 直播业务库/敏感字段契约、权限中间件测试、`api-platform go test ./...`、Vben 构建及差异检查通过。 |
| 生产验收 | 未关闭 | 缺独立假数据运行时、运营/平台截图；区域与商户的直播范围和商户直播供应链仍需按数据范围逐项迁移。 |

## 2026-08-03：统一后台非平台角色默认拒绝

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 服务端总闸 | 已实现 | 新增 `RestrictRoleConsole`，非 `platform` 角色必须命中已迁移的角色路由白名单；Vben 菜单隐藏不再是唯一边界。 |
| 客服边界 | 已实现 | `customer_service` 仅可使用统一认证与 `/customer-service/*`；不能读取财务、系统设置等平台域接口。 |
| 商户边界 | 已实现 | `merchant` 仅放行已使用统一范围解析的商户、订单、售后接口；其他未迁移菜单对应接口保持拒绝。 |
| 运营边界 | 已实现 | `operations` 仅放行文章、秒杀、拼团、预售、券、公告/协议、DIY 等明确运营路由；系统设置与财务拒绝。 |
| 复合角色修复 | 已实现 | 同时拥有 `platform` 的账号不再被区域中间件误限制。 |
| 自动化 | 通过 | 权限回归覆盖客服/商户/运营允许和拒绝路径，以及平台+区域复合角色；`go test ./internal/pkg/middleware ./cmd`、`git diff --check` 通过。 |
| 功能完整性 | 未关闭 | 白名单只代表已实现范围，未迁移的 CRMEB 功能仍需逐项补 API、数据范围、页面和验收证据。 |

## 2026-08-03：用户提现监管迁移

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 业务表迁移 | 已实现 | 统一后台 `/finance/withdraws` 已改读写 `qixi_crm_b_withdrawal_application`；平台启动不再注册引用 `qixi_m_admin_*` 的旧提现处理器。 |
| 角色、按钮与状态机 | 已实现 | 列表、详情、通过、驳回均仅限 `platform`；通过/驳回还须具备统一 RBAC 按钮 `accounts.withdraw.review`，前后端均拒绝越权。仅允许 `applied/reviewing → approved/rejected` 条件更新，重复审核返回冲突，不能伪造已打款。 |
| 收款信息保护 | 已实现 | API 仅返回“已脱敏收款账户”，不读取或输出真实银行卡、手机号等收款敏感信息。 |
| Vben 语义 | 已实现 | 提现页已改为“用户提现”，不再把用户提现描述为返还商户余额。 |
| 中文模拟回归 | 通过单元测试 | 使用订单号样式 `TX-模拟-20260803-001`、虚构用户 ID `10086`，覆盖状态筛选、审批状态展示与收款账户脱敏。 |
| 自动化与构建 | 通过 | `api-platform go test ./...`、`pnpm run build:platform`、`git diff --check`。 |
| 本地数据库/浏览器闭环 | 未关闭 | 需启动本地业务库，以虚构中文用户数据验证申请、平台通过/驳回、重复审核冲突与审计字段，并保存同状态截图。 |

## 2026-08-03：运营营销活动启停闭环

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 路由与菜单一致性 | 已实现 | `operations` 获权的秒杀、拼团、预售、分销、直播、社区、素材接口均列入统一角色白名单；权限回归覆盖新增允许路径。 |
| 活动启停 | 已实现 | Vben 秒杀、拼团、预售监管页以 `platform/operations` 角色和各自 `marketing.*.manage` 按钮码双重控制启停；服务端相同校验继续生效。 |
| 业务表迁移 | 已实现 | 平台秒杀、拼团、预售仓库已改读 `qixi_crm_b_*`；秒杀新增业务表与中文夹具，拼团/预售复用已存在的业务表和夹具，产品元数据统一取 `qixi_crm_b_product_view`。 |
| SQL/夹具回归 | 通过真实 MySQL | 临时库 `qixi_crm_verify_platform_20260803` 成功导入 `01_table.sql`、`05_test_data.sql`；验证秒杀 4 条、预售 3 条中文记录和 `status=1 → 0` 条件停用后恢复。期间修复了不兼容的索引 DDL 和内容夹具括号错误，并以 `business_sql_fixture_contract_test.go` 防回归。 |
| 状态防御 | 通过单元测试 | 三个领域服务在访问数据前拒绝非 `0/1` 状态，避免绕过前端写入非法活动状态。 |
| 高风险操作 | 未开放 | 删除及价格、库存、日期等复杂字段编辑需先完成已下单、成团、尾款订单的影响审计、中文模拟数据回归和浏览器证据；定金支付/尾款总账仍依赖旧交易链路，未完成跨库状态机迁移前不作为生产验收闭环。 |

## 2026-08-03：好友助力业务库边界修复

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 运行表边界 | 已实现（代码） | 助力活动、助力单、助力人模型均改为 `qixi_crm_b_assist*`；`api-platform` 启动时助力仓储显式连接 `businessDB`，不再使用旧 CRMEB 表。 |
| 关联投影 | 已实现（代码） | 商品元数据从 `qixi_crm_b_product_view`、用户昵称从 `qixi_crm_b_user` 获取，保持业务库边界，不跨连管理库或店铺库。 |
| 中文模拟数据 | 已提供 | `sql/business/05_test_data.sql` 已有“真丝印花方巾礼盒”“好友助力满员后可按助力价下单。”及助力完成单，作为 utf8mb4 回归夹具。 |
| 自动化 | 通过 | 新增源码与夹具契约，阻止助力域回写 `qixi_m_*`；`api-platform go test ./...`、`git diff --check` 通过。 |
| 统一后台入口与 RBAC | 已实现（代码） | 新增“好友助力监管”真实 Vben 页面、`marketing.assist` 菜单和 `marketing.assist.manage` 按钮码；仅 `platform/operations` 且拥有按钮码可上架/下架。非平台角色总闸默认拒绝。 |
| 高风险写入收口 | 已实现（代码） | 平台路由只注册 `PUT /assist/actives/:id`，请求体只接受 `is_show: 0|1`；删除、改价、库存和时间调整不在统一后台开放，不能由菜单权限绕过订单影响审计。 |
| 生产验收 | 未关闭 | 仍缺独立假数据运行时、五角色截图，以及助力下单、库存冻结、支付和退款回补状态机演练。 |

## 2026-08-03：平台用户资产流水监管

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 只读监管接口 | 已实现 | 新增 `/finance/user-assets` 与 `/finance/user-assets/summary`，只读业务库 `qixi_crm_b_asset_ledger`，支持资产类型、虚构用户 ID 与分页筛选。 |
| 权限与数据最小化 | 已实现 | 仅 `platform` 角色且拥有 `accounts.user_assets.read` 按钮码可访问；接口只返回用户 ID、资产类型、金额、关联单据与时间，不返回姓名、手机号、账户或支付凭据。 |
| Vben 页面 | 已实现 | 新增“用户资产流水”路由，展示余额、积分、佣金的收支汇总和明细；前端同样在角色与按钮码不足时拒绝加载。 |
| 中文模拟数据 | 通过真实 MySQL | 临时库 `qixi_crm_verify_platform_20260803` 验证虚构用户 `9101` 的余额入账 `66.50`、订单扣减 `-30.00`，汇总为入账 `66.50`、支出 `30.00`、2 笔；数据为 utf8mb4 中文模拟夹具。 |
| 自动化与构建 | 通过 | `nativeledger` 单元测试、`api-platform go test ./...`、`pnpm run build:platform`、`git diff --check` 通过。 |
| 生产验收边界 | 未关闭 | 该项仅覆盖用户资产台账查看，不能替代商户结算、分账、转账或提现打款；后者须完成跨库投影、幂等状态机和资金审计闭环。 |

## 2026-08-04：平台用户详情最小化监管读模型

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 基线覆盖 | 已实现（代码） | 在“用户 / 用户列表”下补齐用户详情、消费记录、余额变动、会员变更、签到记录、浏览记录、用户持有优惠券与推荐关系的统一只读入口；所有修改类操作仍独立列为未关闭项。 |
| API 与权限 | 已实现（代码） | `GET /user-list/:id/detail` 与列表同受 `platform` 角色和 `user.list.read` 按钮码限制；非法 ID 返回 400、不存在用户返回 404，避免以空列表掩盖误查。 |
| 数据最小化 | 已实现（代码） | 返回脱敏手机号、账户投影、最近 20 条订单/资产/会员变更/浏览/优惠券和最近 31 条签到；推荐关系只给上级用户 ID/昵称、直推人数和资格状态，明确排除收货地址、订单收件快照、支付交易号、发票资料及提现账户快照。 |
| Vben 页面 | 已实现（代码） | 用户列表新增“监管详情”抽屉与订单、资产、会员变更、签到、浏览、持有优惠券、推荐关系七个中文标签页；状态、资产类型和会员变更类型均映射为中文展示。 |
| 中文模拟数据 | 已提供 | utf8mb4 夹具用户 `9101` 含“悦享会员”、余额收支、会员升级、连续签到、两条商品浏览记录、两张演示优惠券及虚构上级“晴空漫游者”；无真实地址或支付信息。 |
| 自动化与构建 | 通过 | `go test ./internal/platform/userlist ./cmd`、`pnpm run build:platform`、`git diff --check` 通过。 |
| 生产验收边界 | 未关闭 | 仍需分别完成用户资产、积分、会员等级、优惠券、分销推荐关系等高风险写状态机和幂等审计；还缺隔离假数据运行时的接口联调与五角色浏览器截图。 |

## 2026-08-04：平台用户余额与积分人工调账

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 状态机与幂等 | 已实现（代码） | 平台 `POST /user-list/:id/assets/adjust` 仅接受余额/积分及正负调整数；同一 `asset_type + idempotency_key` 重放不重复入账，金额、用户、中文原因或操作人不一致均返回冲突。佣金、支付和提现均不在该入口开放。 |
| 精度与原子审计 | 已实现（代码） | 余额请求以 JSON 数字解析为固定分整数，拒绝超过两位小数和科学计数法；同一业务库事务锁定 `qixi_crm_b_member_account`，拒绝扣减至负数，随后同时写入 `qixi_crm_b_asset_ledger` 与 `qixi_crm_b_user_asset_adjustment_audit`。 |
| RBAC 与 Vben | 已实现（代码） | 新增“用户资产调整”真实 Vben 页面及 `user.asset.adjust` 按钮码；服务端再次限制为 `platform` 角色。页面声明正负规则、原因必填和外部支付隔离。 |
| 隔离运行时验收 | 已关闭（本地假凭据） | 平台以 0.10 元固定分成功将虚构用户余额从 ¥36.50 调至 ¥36.60；同键重放不重复入账，同键改金额或中文原因均为 409。积分从 268 调至 270；小数积分返回 400，超额余额扣减返回 409。商户、区域、运营、客服调账均为 HTTP 403。 |
| 账本、审计与页面 | 已关闭（本地假凭据） | 直接核验账户积分 270、余额 ¥36.60、佣金 ¥0.00；平台人工账本与审计各 2 条、幂等键均唯一，审计中文原因以 utf8mb4 正常存储。Vben 页面截图确认高风险提示、余额/积分类型和中文表单。 |
| 全局生产验收边界 | 未关闭 | 本条只关闭平台人工余额/积分调账闭环；会员等级、优惠券、推荐关系、充值、提现、支付及其余高风险状态机仍须独立完成验收。 |

## 2026-08-04：平台用户会员等级人工调整

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 新旧边界 | 已实现（代码） | 新入口只使用 `qixi_crm_b_member_account` 与 `qixi_crm_b_member_level{,_log}`；旧 `/users` SVIP 页面仍标记为待迁移，不能作为会员等级生产验收依据。 |
| 状态与幂等 | 已实现（代码） | `GET /user-list/member-levels` 只返回启用等级；`POST /user-list/:id/member-level` 锁定会员账户、验证目标等级，写 `manual` 不可变日志。用户与幂等键唯一，重复请求不重复变更，不一致重放返回冲突。 |
| 业务隔离 | 已实现（代码） | “普通会员”明确存为 `NULL` 等级；人工等级调整不会修改 SVIP、余额、积分、优惠券、佣金或既有订单计价。日志记录中文原因和操作管理员。 |
| RBAC 与 Vben | 已实现（代码） | 新增“用户会员等级调整”页面和 `user.member.adjust` 按钮码，服务端限制 `platform` 角色。 |
| 自动化与构建 | 通过 | `go test ./internal/platform/userlist ./cmd`、`pnpm run build:platform`、`git diff --check` 通过。真实 MySQL 的事务重放、页面截图和五角色拒绝证据待隔离运行时完成。 |

## 2026-08-04：平台用户优惠券发放与撤销

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 状态机与幂等 | 已关闭（本地假凭据） | `POST /user-list/:id/coupons/:coupon_id/issue` 仅对当前启用且处于有效期的模板创建一次 `unused` 用户券；撤销入口仅允许 `unused` 且未关联订单的用户券迁移为 `expired`。命令先锁定用户事实，再按 `action + idempotency_key` 校验重放内容（用户、模板、中文原因、操作人）；两个同键并发发券实测为一次创建、一次重放，变更中文原因复用同键为 HTTP 409。 |
| 业务隔离 | 已实现（代码） | 锁定、支付核销、订单取消恢复仍归订单状态机；本入口不删除用户券、不复活已过期券、不修改锁定/已使用券，也不修改订单、支付或库存事实。 |
| 审计与权限 | 已关闭（本地假凭据） | 同一业务事务写入用户券和 `qixi_crm_b_user_coupon_command_audit`，保留变更前后状态、中文原因、管理员和幂等键；服务端只允许 `platform` 角色及 `user.coupon.manage` 按钮码。平台读取有效“服饰店9折券”并完成发放/撤销；商户、区域、运营、客服对模板读取、发券、撤券均为 HTTP 403。 |
| Vben 与中文数据 | 已关闭（本地假凭据） | 新增“用户优惠券操作”页面，展示有效模板规则和“锁定、已使用或已过期不可由本页修改”的中文高风险提示；虚构用户 `9101` 的“服饰店9折券”由 `unused` 撤销为 `expired`，中文审计原因以 utf8mb4 正确落库，页面截图及浏览器控制台无错误。 |
| 状态拒绝与回归 | 已关闭（本地假凭据） | 同一用户同模板再次发券、撤销后再次发券、撤销已使用券均为 HTTP 409；撤券同键重放为 HTTP 200 且不重复写入，改变撤券中文原因复用同键为 HTTP 409。全量 Go 测试、Vben 平台构建与差异检查待本轮最终回归。 |
| 全局生产验收边界 | 未关闭 | 本条只关闭平台人工发券/撤券命令闭环；领券、下单锁券、订单取消回退、支付核销、退款、库存、积分、佣金和结算仍须按各自状态机独立完成验收。 |

## 2026-08-04：平台用户推荐关系调整

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 关系约束与幂等 | 已关闭（本地假凭据） | `POST /user-list/:id/referrer` 支持设定上级或置空绑定；拒绝本人、缺失用户、循环关系和超过 64 层的异常链。事务按主键顺序锁定目标与候选上级，再读取审计并走关系链；同键并发调整实测为一次创建、一次重放，改中文原因复用键为 HTTP 409。 |
| 原子审计与事实隔离 | 已关闭（本地假凭据） | 关系变更与 `qixi_crm_b_distribution_relation_audit` 在同一业务事务提交；仅改变当前上级绑定，不删除、不重算既有佣金、订单归属或历史审计。虚构用户 9101 完成“9001→9002→清除”闭环，审计 2 条且中文原因 utf8mb4 正常存储。 |
| 并发循环与 RBAC | 已关闭（本地假凭据） | 商户、区域、运营、客服对调整端点均为 HTTP 403；已建立 9101→9002 后，9002→9101 返回 HTTP 409。9001→9002 与 9002→9001 两条反向命令并发时，稳定地仅一条 HTTP 200、另一条 HTTP 409，未形成循环。 |
| Vben 与构建回归 | 已关闭（本地假凭据） | “用户推荐关系调整”真实页面与 `user.referrer.manage` 按钮码均已截图复核；页面显示中文循环关系约束、留空清除上级语义、原因必填和历史佣金/订单事实不重算提示，浏览器控制台无错误。`go test ./...`、`pnpm run build:platform` 和 `git diff --check` 均通过。 |

## 2026-08-04：平台用户分组归属

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| CRMEB 基线缺口 | 已补齐（代码） | 在既有“用户分组”字典 CRUD 之外，新增用户单个及 1–100 人批量归属调整；`group_id=0` 明确代表移出运营分组，覆盖基线中的用户分组编辑与批量编辑。 |
| 原子命令与幂等 | 已关闭（本地假凭据） | `POST /user-list/:id/group` 和 `POST /user-list/groups/assign` 去重并稳定排序用户 ID；事务先锁定所有用户和目标有效分组，再读审计并更新 `qixi_crm_b_user.group_id`、写入 `qixi_crm_b_user_group_command_audit`。同键重复数组不同顺序的并发请求实测为一次创建、一次重放；中文原因或操作人不一致会冲突。 |
| 业务边界与权限 | 已关闭（本地假凭据） | 分组只属于运营归属；命令不改变用户登录、会员等级、订单、资产或佣金。平台可读取有效“新品体验用户”分组并批量分配/清除；商户、区域、运营、客服对读取和批量写入均为 HTTP 403。 |
| 批量回滚与中文审计 | 已关闭（本地假凭据） | `[9001,9999]` 部分用户不存在及不存在目标分组均为 HTTP 404，未写命令审计；9001、9002 完成“纳入新品体验用户→移出运营分组”，既有 9101 的“CRM Live精选会员”分组保持不变。审计仅 2 条、键唯一，中文原因以 utf8mb4 存储。 |
| Vben 与构建回归 | 已关闭（本地假凭据） | “用户分组归属”页面截图确认 1–100 批量输入、中文逗号/换行规则、未分组清除语义和业务事实隔离提示，浏览器控制台无错误。`go test ./...`、`pnpm run build:platform` 和 `git diff --check` 均通过。 |

## 2026-08-04：平台用户标签归属

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| CRMEB 基线缺口 | 已补齐（代码） | 在既有标签字典 CRUD 和单用户打标之外，新增用户单个及 1–100 人批量标签整体替换；空集合代表清除运营标签，覆盖基线中的用户标签编辑与批量编辑。 |
| 原子命令与幂等 | 已关闭（本地假凭据） | `POST /user-list/:id/labels` 和 `POST /user-list/labels/assign` 会验证 1–100 个用户、至多 50 个有效标签；事务先按稳定顺序锁定用户与标签，再整体替换 `qixi_crm_b_user_label_relation`、写入 `qixi_crm_b_user_label_command_audit`。重复用户/标签及不同数组顺序会规范化；同键并发实测为一次创建、一次重放，中文原因或操作人不一致会冲突。 |
| 业务边界与权限 | 已关闭（本地假凭据） | 标签仅用于运营分层，不改变登录、会员等级、订单、资产或佣金。平台可读取“高频复购用户”等有效标签并整体替换/清除；商户、区域、运营、客服对读取和批量写入均为 HTTP 403。 |
| 批量回滚与中文审计 | 已关闭（本地假凭据） | `[9001,9999]` 部分用户不存在及不存在标签均为 HTTP 404，未写标签关系或命令审计；9001、9002 完成“高频复购 + 香氛兴趣→清除”闭环，9101 既有两枚标签不变。审计仅 2 条、键唯一，中文原因以 utf8mb4 存储。 |
| Vben 与构建回归 | 已关闭（本地假凭据） | “用户标签归属”页面截图确认整体替换、留空清除、中文输入、1–100 批量范围与业务事实隔离提示，浏览器控制台无错误。`go test ./...`、`pnpm run build:platform` 和 `git diff --check` 均通过。 |

## 2026-08-04：平台用户启停与 C 端会话失效

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 状态与幂等 | 已通过本地运行时验收 | `POST /user-list/:id/status` 仅接受启用/停用；事务先锁定用户事实、再读取同键审计，并核验用户、目标状态、中文原因与操作人。状态实际变化才递增 `auth_version` 并写入 `qixi_crm_b_user_status_command_audit`；同键并发实测仅一次创建、其余重放，改中文原因返回冲突。 |
| 会话安全边界 | 已通过本地运行时验收 | `api-business` 的所有 C 端受保护路由比较用户 `status` 与 JWT `identity_version`。本地假用户完成 `1→0→1`、身份版本 `1→2→3` 闭环：停用时版本 1、2 令牌均被拒绝；重新启用后版本 1、2 仍被拒绝，只有版本 3 可访问。停用不取消既有订单、退款、余额、优惠券或佣金事实。 |
| RBAC 与 Vben | 已通过本地运行时验收 | 新增“用户启停”页面及 `user.status.manage` 按钮码，服务端限制 `platform` 角色；平台令牌可完成命令，商户、区域、客服、运营令牌均为 HTTP 403。Vben 截图确认中文启停表单、身份版本失效说明、交易事实隔离警示和原因必填，页面控制台无错误。 |
| 自动化与构建 | 通过 | 新增默认跳过外部库的可复用 MySQL 集成测试：显式提供本地临时 DSN 时运行 `TestStatusIntegrationDisableAndRBAC`、`TestStatusIntegrationReenable`、`TestCUserSessionIntegrationDisabled`、`TestCUserSessionIntegrationReenabled`。本轮只使用隔离 MySQL 与临时假 JWT，没有输出、写入或提交真实凭据。 |

## 2026-08-04：平台用户新增、资料维护与密码重置

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| CRMEB 基线覆盖 | 已通过后端运行时验收 | 新增本地 PC 用户、维护用户公开资料、重置本地 PC 密码三个独立命令，分别对应基线“用户添加、用户编辑、用户修改密码”。三项均已完成隔离 MySQL 命令级验收，页面独立截图仍持续补录。 |
| 身份与密码安全 | 已通过本地运行时验收 | 新增用户密码与重置密码仅被 bcrypt 处理；数据库命令审计只记录请求摘要、中文原因、操作人和幂等键，绝不保存密码、凭据或密码派生摘要。密码重置先锁定用户并绑定操作人；因不能安全持久化密码指纹，同键再次提交一律冲突而不重放或覆盖口令。六个同键并发请求实测为一次成功、五次冲突；旧 bcrypt 口令失效、新口令有效，`auth_version` 从 1 升至 2。 |
| 资料最小化 | 已通过本地运行时验收 | 资料维护仅允许昵称、头像、性别和简介；头像只允许空值、`https://` 或 `/demo/` 受控资源。并发同键更新先锁用户，实测一次执行、其余安全重放；改中文原因重放返回冲突。登录账号、手机号、收货地址、证件、支付与提现信息不在此入口暴露或编辑。 |
| RBAC 与 Vben | 已通过 | 页面码 `user.create` 与按钮码 `user.create.execute` 已拆分，修复唯一菜单码冲突导致的“新增用户”路由重定向；`user.profile.manage`、`user.password.reset` 分别对应独立按钮码。三种命令均仅接受平台角色；商户、区域、客服、运营均为 HTTP 403。以本地虚构中文管理员登录后，“新增用户、用户资料维护、用户密码重置”三页均截图渲染，浏览器控制台无错误。 |
| 自动化与构建 | 通过 | 新增 `TestCreateAndProfileIntegrationRBACAndAtomicity`、`TestPasswordResetIntegrationRBACAndSingleUse` 与 `TestCUserSessionIntegrationPasswordReset`：五个同键并发创建仅一次成功、四次冲突；账号重复返回冲突；资料更新、中文审计及 bcrypt 口令边界均已核验。仅使用隔离 MySQL 和进程内假令牌，结束后已清理固定验收数据。 |

## 2026-08-04：平台批量设置推广员资格

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| CRMEB 基线覆盖 | 已实现（代码） | 新增 `POST /user-list/promoters/assign`，覆盖“批量设置分销员”；一次命令接受 1 至 100 个去重、稳定排序的用户 ID。 |
| 事实边界与幂等 | 已通过本地运行时验收 | 命令仅写 `qixi_crm_b_distribution_promoter.status` 与命令审计；事务先锁定规范化用户集合，并校验操作人、集合、状态与中文原因。六个同键并发请求实测一次执行、五次重放；改原因复用键冲突。不重算、不删除或结算 `qixi_crm_b_commission_ledger`，不修改订单及推荐关系。 |
| RBAC 与 Vben | 已通过 | `user.promoter.manage` 仅平台角色可用；商户、区域、客服、运营均为 HTTP 403。Vben“批量设置推广员”页面截图确认中文批量输入、1–100 限制、资格启停与佣金事实隔离提示，干净浏览器会话控制台无错误。 |
| 中文模拟数据与自动化 | 通过 | 隔离库中两个固定中文假用户完成“开通→停用”闭环；部分集合含不存在用户返回 HTTP 404 且未产生资格行。既有假佣金流水保持 `¥18.88 / available`。验收后固定用户、资格、账本与审计均已清理。 |

## 2026-08-04：平台用户站内图文通知

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| CRMEB 基线处理 | 已实现受控替代 | CRMEB 的用户图文发送依赖外部公众号能力；在“不使用真实凭据”约束下，后台仅创建 C 端可见的站内图文通知，明确不调用或伪装微信、短信、邮件、IM 等外部通道。外部渠道投递不在本项关闭范围。 |
| 数据与幂等 | 已通过本地运行时验收 | 创建 `qixi_crm_b_notification` 与 `qixi_crm_b_user_notification_command_audit`；事务先锁定收件人、再读审计，并绑定操作人、标题、全文、封面和中文原因。六个同键并发请求实测一次创建、五次重放；改中文原因复用键为 HTTP 409，通知与审计均只保留一条。审计不保存外部凭据。 |
| 内容安全与 RBAC | 已通过本地运行时验收 | 正文限制 2000 字符，封面仅接受空、`https://` 或 `/demo/` 受控地址；不存在用户为 HTTP 404、非受控封面为 HTTP 400 且不写入。新接口及 `user.notification.send` 按钮码只允许平台角色，商户、区域、客服、运营请求均为 HTTP 403。 |
| Vben 与中文模拟数据 | 已通过 | 以虚构中文平台账号登录后真实打开“站内图文通知”表单，发送本地中文图文提醒，页面提示“已创建并写入审计记录”；截图采集成功，控制台无警告/错误。通知明确仅为 C 端站内消息，不调用公众号、短信、邮件或 IM 外部通道。 |
| 自动化与生产验收边界 | 本项已关闭 | 新增 `TestNotificationIntegrationRBACAndReplay` 覆盖五角色、缺失收件人、并发重放、审计、载荷、冲突键和非法封面；隔离 MySQL 的固定中文假数据和页面假通知均在验收后精确清理。外部公众号图文投递不属于本项功能，若未来启用需另行授权、接入受控凭据并独立验收。 |

## 2026-08-04：平台用户信息导出

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| CRMEB 基线覆盖 | 已通过本地运行时验收 | 新增“导出脱敏信息”入口与 `POST /user-list/export`，支持用户 ID、昵称、状态筛选，最多返回 5000 行 UTF-8 CSV。 |
| 隐私与导出安全 | 已通过本地运行时验收 | CSV 仅含用户 ID、昵称、脱敏手机号、状态、余额、积分、等级与注册时间；不含地址、身份凭据、支付/提现账户、订单或发票资料。5001 条隔离中文假数据实测返回 5000 条且 `truncated=true`；公式型昵称以单引号转义，手机号为脱敏形式。 |
| 审计与 RBAC | 已通过本地运行时验收 | 每次导出在返回文件前写入 `qixi_crm_b_user_export_audit`，只保留查询指纹、行数、中文原因和操作人；`user.list.export` 按钮码及接口均限制平台角色，商户、区域、客服、运营均为 HTTP 403。 |
| 自动化与生产验收边界 | 后端已关闭，页面下载待补 | `TestExportIntegrationRBACTruncationAndCSVSafety` 已覆盖筛选、5000 行真实截断、BOM、公式转义、手机号脱敏、审计与五角色。固定假数据和审计在测试后清理；Vben 的浏览器实际下载流程将在下一轮补充。 |

## 2026-08-04：区域代理资格撤销

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| CRMEB 基线覆盖 | 已实现受控删除 | CRMEB 的“代理删除”在七禧实现为资格撤销：`DELETE /business-zone-agents/:id` 将状态置为 `revoked`，不硬删代理、审核或结算资料。 |
| 事实保护与幂等 | 已通过本地 MySQL 事务验收 | 事务先锁代理行，再检查 `(代理、动作、幂等键)` 审计并绑定操作人；六个同键并发实测一次撤销、五次重放，中文原因或操作人变化均冲突。仅已通过代理可撤销；待审/驳回、已关联区域、佣金余额非零或仍绑定统一后台账号均拒绝。首次撤销写入原/目标状态、中文原因、操作人和幂等键。 |
| RBAC 与 Vben | 已通过隔离运行时验收 | `TestAgentRevocationHTTPRBACAcrossFiveRoles` 串联统一后台 JWT、会话版本、角色、按钮码和路由：平台撤销为 HTTP 200，商户、区域、运营、客服均为 HTTP 403，未登录为 HTTP 401。真实 Vben 以虚构平台账号执行中文原因撤销，列表变为“已撤销”，且待审/已撤销行不再展示不可执行的撤销或编辑按钮。 |
| 代理选项与关联商户 | 已实现（代码） | 新增受同一 RBAC 保护的代理选项及关联商户接口；选项仅返回代理 ID、名称、类型，商户视图仅返回商户 ID、名称、区域、状态。页面不展示联系人、地址、结算账户或代理支付资料。 |
| 自动化与生产验收边界 | 本项已关闭 | `TestRevokeAgentIntegrationGuardsAndReplay` 覆盖待审、余额、区域关联阻断及并发重放；`TestAgentRevocationHTTPRBACAcrossFiveRoles` 覆盖五角色 HTTP 拒绝链。隔离 MySQL 使用“七禧区域代理页面验收”等 UTF-8 假数据，真实 Vben 截图确认撤销后的状态与按钮收口；测试账号、代理与审计均已清理。本项不替代代理审核、区域绑定和佣金结算等其他功能的独立验收。 |

## 2026-08-04：区域代理统一后台账号绑定与密码重置

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| CRMEB 基线覆盖 | 已实现 | 对齐“代理人员 / 重置密码”：`POST /business-zone-agents/:id/password` 只作用于与 `qixi_crm_a_admin_user.circle_agent_id` 一对一绑定的统一后台区域账号，不访问 C 端用户或任何结算资料。 |
| 绑定完整性 | 已实现（代码） | 管理员设置保存时仅允许非平台的 `region` 角色绑定已审核通过的代理；数据库为 `circle_agent_id` 增加可空唯一索引，重复历史绑定不会被静默迁移。Vben 改为只读已通过代理的下拉选择。 |
| 密码与会话安全 | 已通过本地 MySQL 运行时验收 | 新密码仅用于 bcrypt 计算；不回显、不回传、不写入审计，也不保存派生指纹。隔离中文假账号实测旧 bcrypt 口令失效、新口令有效，`auth_version` 从 1 递增至 2；统一后台后续鉴权会据此拒绝旧会话。 |
| 幂等与审计 | 已通过本地 MySQL 运行时验收 | 专用审计表仅保存代理 ID、受影响后台账号 ID、中文原因、操作人和幂等键；因审计不保存口令或派生摘要，密码重置采用单次提交语义：同代理同键再次提交一律 HTTP 400 冲突，避免不同新口令被伪装为重放成功。代理仍绑定后台账号时不可撤销。 |
| 代理设置页面 | 已实现（代码） | 补齐 `/business-zones/settings` 菜单、Vben 页面与只读接口；显示服务端聚合的状态数量、审核/绑定规则、密码边界、凭据只写策略和撤销阻断条件，不伪造 CRMEB 未定义的写配置。 |
| 会话即时失效 | 已实现（代码） | 新增统一后台会话版本中间件，受保护接口均比对账号启用状态、`auth_version` 与 `data_scope_version`；重置密码、禁用账号或更新数据范围后旧令牌在下一次任意请求被拒绝。 |
| 中文模拟数据 | 已提供 | 既有 `sql/admin/05_test_data.sql` 提供“区域主管王小明”等明显虚构中文代理和空结算资料；后台账号与密码刻意不写入夹具，须通过受控管理员设置创建，避免仓库保存任何凭据。 |
| 自动化与生产验收边界 | 本项已关闭 | `TestResetAgentPasswordIntegrationSingleSubmit` 验证 bcrypt 更新、旧口令失效、`auth_version 1→2` 与同键单次提交；`TestAgentPasswordResetHTTPRBACAndSessionInvalidation` 串联统一后台 JWT、会话版本、角色和按钮码：平台为 HTTP 200，商户、区域、运营、客服均为 403，未登录及被重置账号的旧会话均为 401。真实 Vben 以“七禧代理密码页面验收”等 UTF-8 虚构数据提交中文原因后显示“后台密码已重置，该代理旧后台会话已失效”；审计仅一条且不保存口令或派生摘要。临时账号、代理与审计均已清理。 |

## 2026-08-04：退款监管日志、审计导出与统一后台 RBAC

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| CRMEB 基线覆盖 | 已实现（代码） | 平台退款页补齐“日志、导出”操作，现覆盖列表、详情、日志、审核、导出；导出列表/下载以一次受审计的浏览器 CSV 下载实现，不另建无法回收的文件副本。 |
| 统一后台授权 | 已实现（代码） | 审核、拒绝、日志、导出均使用 `RequireAdminRoles("platform")` 和 `RequireAdminMenu` 校验 `qixi_crm_a_role_menu`；已移除退款处理对旧 `qixi_m_admin_*` 身份服务的依赖。区域、商户、运营、客服均不能调用日志或导出接口。 |
| 范围与隐私 | 已实现（代码） | 日志先按退款归属商户执行数据范围检查；导出同样复用范围查询且只允许平台角色。CSV 仅含退款、订单、商户、店铺、类型、金额、状态和时间，不含用户身份、退款原因或退货物流；退款单号同样做公式注入防护。 |
| 审计与中文夹具 | 已实现/已提供 | 新增 `qixi_crm_b_refund_export_audit`，只记录筛选摘要、行数、中文原因、操作人和时间；`sql/business/05_test_data.sql` 增加“用户已提交退货物流，等待商户确认收货”的 UTF-8 中文流转夹具。 |
| 自动化与生产验收边界 | 部分关闭 | `TestRefundHTTPRBACAndMerchantRegionScopes` 已在隔离三库 MySQL 实测平台、商户、区域、运营、客服和无范围账号的退款列表/详情/审批边界，中文数据通过 HTTP JSON 回读。平台可读取中文状态流转日志、按状态导出两笔退款并写入一条含中文原因和行数的 `qixi_crm_b_refund_export_audit`；导出正文含字段标题但不含退款原因。商户、区域、运营、客服读取日志或导出均为 403。真实 Vben 以虚构平台账号显示 `refunding` 为“退款处理中”，操作列只保留详情/日志、无同意/拒绝按钮，浏览器控制台无错误；页面截图为 `/tmp/ecrm-refund-ui-acceptance.png`。同时修复空店铺排行榜返回 `null` 时控制台的 `null.length` 渲染异常：后端稳定输出 `[]`，前端兼容历史 `null`，新会话控制台无错误。`api-platform go test ./...`、`admin-platform pnpm run build:platform` 与 `git diff --check` 通过。退货寄回→商户确认→退款回调同次链路已另行在隔离双服务 HTTP 关闭；其余角色页面截图仍未完成，不以本项代表整个后台生产验收完成。 |

## 2026-08-04：商户退款日志、物流快照与导出

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| CRMEB 基线补齐 | 已实现（代码） | 商户退款页新增操作日志、退货物流快照和导出；列表、详情、审核、驳回、确认收货仍沿用既有受状态机保护的实现。 |
| 店铺隔离与 RBAC | 已实现（代码） | 三个接口均先按 JWT 的 `store_id` 查询退款归属，再访问 `qixi_crm_b_refund_event` 或退货物流；分别受 `refund.log`、`refund.express`、`refund.export` 的 `qixi_crm_m_*` 权限控制。 |
| 导出边界 | 已实现（代码） | CSV 最多 5000 行，仅包含退款号、订单、类型、金额、状态和时间；不含买家 ID、售后原因或物流；退款号做公式注入防护，中文以 UTF-8 BOM 输出。 |
| 自动化与生产验收边界 | 部分关闭 | `TestConfirmReturnHTTPIntegration` 已在隔离双库以生产同构店铺 JWT、`X-AppId`、会话与三项按钮权限实测确认收货、中文备注和受控隐藏；备注/隐藏同键重放成功、同键改内容为 409、跨店备注为 404，隐藏后本店详情为 404。业务退款仍为 `refunding`、退款交易仍一条，证明隐藏未触碰退款事实。标准 `pnpm run build:merchant` 仍受既有未定义 `@iconify/json` catalog 配置阻断；五角色界面截图待关闭。 |

## 2026-08-04：商户退款备注与受控隐藏

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 备注与幂等 | 已实现（代码） | `POST /refunds/:id/remark` 只写入商户库 `qixi_crm_m_aftersale_action`。同账户、同售后、同键、同内容重放成功；同键不同内容冲突；备注不会改写退款申请原因、状态或支付记录。 |
| 删除语义 | 已实现受控替代 | CRMEB 的删除实现为 `DELETE /refunds/:id` 写入 `qixi_crm_m_refund_hidden`：仅隐藏本店后台视图，平台监管、业务退款事实、支付回调和资金交易不被删除或篡改。 |
| 店铺隔离与授权 | 已实现（代码） | `refund.remark` 与 `refund.delete` 均检查 `qixi_crm_m_*` RBAC。隐藏退款从该店列表、详情、日志、物流、导出和后续处理均不可见，不能通过直接 ID 继续操作。 |
| 中文模拟数据与自动化 | 部分关闭 | `TestConfirmReturnHTTPIntegration` 在隔离 MySQL 以“仓库已核验退货包裹外观，等待退款回调。”和“本店已完成售后归档，隐藏后台视图。”验证备注、隐藏与重放；改同键中文内容均为 409，跨店备注为 404，隐藏后本店详情为 404。测试后备注、隐藏、退款和交易计数均清理为 0。浏览器截图与真实商户 Vben 构建阻断仍待关闭。 |

## 2026-08-03：店铺结算申请、审核与监管投影

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 平台/区域只读监管 | 已实现 | 新增 `qixi_crm_a_merchant_settlement_view` 与 `/finance/merchant-settlements{,/summary}`；平台读取全量后台投影，区域只按 `qixi_crm_a_data_scope.region` 的 `region_ids` 查询，不直接连接店铺库。 |
| 申请、审核与打款命令 | 已实现（代码） | 商户仅能对 `bill_frozen` 账期申请；平台仅通过受控 NATS 命令执行通过、拒绝、登记内部打款凭证。商户端以原状态条件更新，并在同一事务写 `qixi_crm_m_outbox`；审核、打款各有独立幂等键。平台不跨库写结算事实。 |
| 权限与界面 | 已实现 | 新增“店铺结算监管”Vben 页面、`accounts.merchant_settlement.read` 与 `accounts.merchant_settlement.review` 按钮码；区域只读，只有平台角色可显示通过、拒绝、登记打款操作。 |
| 中文模拟数据 | 已提供，待真实库导入 | utf8mb4 夹具包含区域 10 的“七禧演示店铺”待平台审核结算 `1280.50` 与区域 20 的“七禧居家优选店”已打款结算 `960.00`；静态夹具契约测试覆盖。Docker MySQL 当前需要密码，按“不使用真实凭据”约束未读取或使用凭据，故未标记为真实 MySQL 通过。 |
| 自动化与构建 | 通过 | 商户与平台结算状态、命令参数、幂等键及区域范围单元测试通过；`api-merchant go test ./...`、`api-platform go test ./...`、`pnpm run build:platform`、`git diff --check` 通过。 |
| 生产验收边界 | 未关闭 | 尚缺使用独立假凭据的三库/NATS 运行时联调、命令重放和失败恢复演练、五种角色登录截图，以及 CRMEB MER v4.0 其余功能项的逐项闭环；不得以本条代码完成替代整体生产验收。 |

> 当前台账不能证明统一后台 1333 项 CRMEB 操作已经完成或达到生产验收。后续每项必须按本文件“关闭标准”补齐证据。

## 2026-08-04：运营预售监管详情与时间窗保护

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| CRMEB 基线与 RBAC | 已通过本地运行时验收 | 预售监管展示商品/商户、全款或定金类型、预售价、定金与尾款、活动与尾款时间窗、库存、已售、发货、展示及启停状态；列表、详情、启停均限制 `platform`/`operations` 和 `marketing.presell.manage`。此前列表/详情缺少服务端权限检查已修复，商户、区域、客服真实路由均为 HTTP 403。 |
| 订单快照与写入边界 | 已通过本地运行时验收 | 平台接口仅接受 `status` 的 0/1 启停；只传价格被 HTTP 400 拒绝，携带价格/库存的启停载荷不会改写原 ¥299.00 或库存 12。停用活动后报价拒绝，避免新订单继续引用停用活动；已有定金或尾款订单的价格和时间窗不由后台修改。 |
| Vben、中文模拟数据与自动化 | 已通过本地运行时验收 | 平台虚构账号真实打开“预售监管”，查看“中文预售页面验收商品”的 ¥299.00、¥59.00/¥240.00、尾款窗与快照提示，并完成启用→停用，页面显示“活动已停用”，浏览器无 error。`TestPresellHTTPRBACAndStatusOnlyUpdate` 覆盖五角色、篡改载荷与停用报价拒绝；中文夹具以 utf8mb4 字节核验。 |
| 清理与全局生产验收边界 | 未关闭 | 本轮临时账号、活动、登录/操作审计、API/Vite 和 MySQL 均会清理。本条只关闭预售监管启停与报价拒绝；定金支付、尾款支付、库存、退款、结算及其跨服务回调仍须独立完成状态机验收。 |

## 2026-08-04：运营好友助力监管筛选与详情

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 筛选一致性与读权限 | 已通过本地运行时验收 | 读路由与写路由统一要求 `platform/operations` 及 `marketing.assist.manage`，堵住此前列表/详情可被直接请求绕过的漏洞。运营按 `mer_id=1` 查询到虚构中文活动；商户、区域、客服列表均为 HTTP 403。`0` 或非数字商户 ID 仍为 HTTP 400。 |
| CRMEB 监管操作与写入收口 | 已通过本地运行时验收 | 详情弹窗展示助力价、所需人数、单人次数、库存、活动窗及展示/启停状态；平台/运营仅能按按钮码上下架。价格单字段载荷返回 HTTP 400；混入价格、库存的上下架载荷只会改变 `is_show`，原 ¥199.00 和库存 12 保持不变。 |
| 隐私与资金边界 | 已实现（代码） | 详情不返回发起人、帮扶人或联系方式；不开放已发起助力单相关的价格、库存、完成条件与时间编辑。 |
| 中文模拟数据、Vben 与自动化 | 已通过本地运行时验收 | `TestAssistHTTPRBACAndVisibilityOnlyUpdate` 使用真实 MySQL 覆盖五角色、筛选、详情、篡改载荷及隐藏后不可发起助力。真实 Vben 以“中文好友助力页面验收商品”完成详情与上架→下架，页面显示“已下架/上架”，浏览器无 error；写入前以 utf8mb4 字节值核验中文。 |
| 生产验收边界 | 未关闭 | 本条仅关闭活动监管的读权限、详情、上下架与新助力发起拒绝。助力成团、下单、库存冻结、支付、退款回补与结算仍须按各自状态机完成跨服务验收。 |

## 2026-08-03：分销监管读模型

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 推广员与佣金监管 | 已实现（代码） | 新增 `/distribution/promoters`、`/distribution/commissions`、`/distribution/summary`，只读业务库 `qixi_crm_b_distribution_*` 与 `qixi_crm_b_commission_ledger`，返回推广资格、直推人数及各佣金状态汇总，不返回个人资料或收款信息。 |
| RBAC 与角色边界 | 已实现（代码） | `marketing.spread.read` 按钮码授予平台与运营；区域、商户、客服均被服务端拒绝。运营角色白名单仅开放 `/distribution/*` 读接口。 |
| Vben 页面 | 已实现（代码） | “分销监管”改为推广员、佣金流水两个真实分页列表与汇总卡，不再调用旧 `/spread/*` 接口。 |
| 旧表隔离 | 已实现（代码） | 统一后台已撤销旧 `/spread/logs`、`/spread/bills` 路由注册，避免通过旧 promotion repository 查询退役表。 |
| 中文模拟数据、RBAC 与 Vben | 已通过本地运行时验收 | `TestDistributionHTTPRBACAndReadModel` 在真实 MySQL 以虚构中文后台账号与佣金 6.50/12.80/3.20 覆盖运营/平台读取、商户/区域/客服拒绝、状态白名单、指定推广员直推数及佣金流水。全局汇总允许含既有基线夹具，不写死测试轮金额。真实 Vben 显示汇总、推广员与佣金流水两个页签，浏览器无 error。 |
| 生产验收边界 | 未关闭 | 本条关闭监管读模型，不开放资金写入。分销员资格调整、等级、海报、礼包、提现以及佣金结算仍须按各自状态机独立验收，不能据此条宣称分销模块完整。 |

## 2026-08-03：用户提现审核与打款登记

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 状态机与幂等 | 已通过本地运行时验收 | 隔离 MySQL 的真实注册路由完成 `applied → approved → paid`。相同 `idempotency_key + payout_reference` 安全重放为 200；相同键改为不同内部凭证返回 409，修复了历史上会静默接受篡改凭证的缺口。 |
| RBAC 与最小数据 | 已通过本地运行时验收 | 平台审核及登记打款为 HTTP 200；商户、区域、运营、客服对列表及登记打款均为 HTTP 403。响应只输出“已脱敏收款账户”，不会读取或回显 `account_snapshot`，也不接入外部付款渠道。 |
| Vben 与中文模拟数据 | 已通过本地运行时验收 | 平台虚构账号真实登录“提现审核”，对 `WD-UI-中文-001` 的 ¥66.50 虚构微信提现登记“本地模拟打款凭证-UI-001”，页面显示“已打款”；中文单号和审核备注以 utf8mb4 字节核验，浏览器控制台无 error。 |
| 自动化、清理与全局边界 | 已通过本地运行时验收 | `TestWithdrawHTTPRBACAndPayoutIdempotency` 覆盖五角色、审核、重放和篡改冲突；本轮最终将执行平台全量 Go、Vben 构建与差异检查。临时账号、提现申请、登录记录、临时 API/Vite 与 MySQL 均在验收后清理。本条不代表真实付款、提现资金冻结/释放、支付回调或全后台生产验收完成。 |

## 当前高风险迁移顺序

1. 商户结算、分账与资金投影：用户提现已迁至 `qixi_crm_b_withdrawal_application`；商户结算等财务域仍须移除 `qixi_m_admin_*` 依赖，并按资金状态机、幂等和数据范围逐项验收。
2. 商户入驻申请：建立申请记录到后台监管投影的范围字段，再开放商户/区域审核操作；受控开店命令已实现，尚待本地 NATS/三库闭环验收。
3. 运营内容、DIY、营销与素材：按操作级菜单权限和 `operations` 数据范围逐页关闭。
4. 客服 18 项：移除旧独立链路，统一为后台 JWT、授权店铺队列和 pte-live-im 受控接口。

## 2026-08-04：SVIP 监管统一 RBAC 与隐私收敛

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 统一后台权限 | 已实现（代码） | SVIP 列表与状态设置由旧身份菜单校验迁移到 `RequireAdminRoles("platform")` 与 `qixi_crm_a_*` 按钮码 `user.svip.manage`；非平台角色不再可通过直接接口访问。 |
| 最小化个人数据 | 已实现（代码） | API 不再返回原始账号或手机号，只返回 `phone_masked`；Vben 列表和编辑弹窗不再回退展示账号、原始手机号。 |
| 自动化与构建 | 通过静态验证 | 手机号脱敏单测、`api-platform go test ./internal/platform/svip ./cmd` 与 `admin-platform pnpm run build:platform` 通过。 |
| 隔离运行时验收 | 已关闭 | 平台可访问，运营可访问套餐、权益、购买记录；商户、区域、客服直接访问四个 SVIP 端点均为 HTTP 403。SVIP 用户人工状态、支付回调、过期任务和折扣计价仍属于独立高风险验收项。 |

## 2026-08-04：SVIP 会员类型与购买记录监管

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| CRMEB 基线补齐 | 已实现（代码） | 新增“会员类型”与“会员记录”两个 Vben 入口，分别覆盖会员类型列表/新增/编辑/启停和会员购买记录列表/状态筛选/统计；人工用户 SVIP 状态仍保留在原“付费会员”页。 |
| 资金与权益边界 | 已实现（代码） | 后台仅维护未来可售 `qixi_crm_b_svip_plan`；不提供删除、补单、改价、人工置已支付或人工发放权益。`qixi_crm_b_svip_order` 只读，支付回调仍应作为唯一推进订单和授予权益的幂等事务。 |
| RBAC | 已实现（代码） | 类型接口限 `platform`/`operations` 并要求 `user.svip.plan.manage`；记录接口要求 `user.svip.record.read`；运营总闸仅开放 `/svip/`，原手工 SVIP 授权继续严格限平台。商户、区域、客服没有相应菜单或接口权限。 |
| 中文模拟数据与自动化 | 通过静态验证 | `sql/business/05_test_data.sql` 提供“SVIP 月度会员”等 UTF-8 类型及待支付、已关闭的虚构订单，刻意不伪造成功支付或渠道流水。`api-platform go test ./internal/platform/svip ./internal/pkg/middleware ./cmd`、`api-platform go test ./internal/domain`、`admin-platform pnpm run build:platform`、目标文件 `git diff --check` 均通过。 |
| 隔离运行时验收 | 已关闭 | 独立假凭据 MySQL 中平台、运营均可读取；商户、区域、客服拒绝。运营实际新增“验收专用体验会员”；中文 Vben 页面展示套餐、权益与只读订单统计。支付回调重放、过期任务和折扣计价仍由独立高风险链路验收。 |

## 2026-08-04：平台用户搜索记录监管

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| CRMEB 基线补齐 | 已实现（代码） | 新增“用户搜索记录”真实 Vben 页面，覆盖筛选、分页、按用户清理及 CSV 导出；不再以无接口的菜单占位替代 `搜索记录 / 清除 / 导出` 基线操作。 |
| 隐私、删除与审计 | 已实现（代码） | 业务表仅保存用户 ID、关键词、来源和时间，不保存账号、手机号、地址、设备标识；清理只写 `deleted_at`，必须输入中文理由和幂等键，并在 `qixi_crm_b_user_search_record_clear_audit` 保存用户、理由、操作人和清理数量。导出记录筛选哈希、行数、理由、操作人，不复制关键词。 |
| RBAC 与导出安全 | 已实现（代码） | 三项接口均严格限平台角色，并分别要求 `user.search_record.read`、`user.search_record.clear`、`user.search_record.export`；CSV 最多 5000 行、带 UTF-8 BOM，并转义以 `= + - @` 开头的用户输入关键词。 |
| 中文模拟数据与自动化 | 通过静态验证 | 业务夹具含“夏季亚麻衬衫”“居家香薰礼盒”等 UTF-8 虚构搜索记录；`api-platform go test ./internal/platform/usersearch ./internal/pkg/middleware ./cmd`、`admin-platform pnpm run build:platform` 通过。 |
| 隔离运行时验收 | 已关闭（本地假凭据） | 平台查询 3 条中文搜索记录、按“夏季”筛选为 1 条；商户、区域、运营、客服对读取、清理、导出均为 HTTP 403。平台清理虚构用户两条记录后，同键重放返回原清理数 2，不同请求复用键返回 409，清理记录不再可见。 |
| 导出审计与页面 | 已关闭（本地假凭据） | 平台按“儿童”导出 1 条记录，返回 UTF-8 BOM CSV 且包含“儿童雨靴”；直接查询确认 2/3 条记录已逻辑删除、清理审计 1 条且清理数 2、导出审计 1 条且行数 1。Vben 截图确认中文按钮、最小化字段和本地可读搜索时间。 |
| 全局生产验收边界 | 未关闭 | 本条只关闭搜索记录监管的隔离运行时证据；用户资产、积分、优惠券、推荐关系、订单及其余 CRMEB 基线仍须按各自状态机和数据范围逐项验收。 |

## 2026-08-04：平台普通会员等级管理

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| CRMEB 基线补齐 | 已实现（代码） | 新增“会员等级管理”真实 Vben 页面与等级列表、新增、编辑、启停、删除入口，覆盖原有“用户等级 / 等级管理”的配置职责；用户当前等级调整继续使用独立审计命令。 |
| 数据与并发边界 | 已实现（代码） | `qixi_crm_b_member_level` 增加 `version` 和 `deleted_at`；更新要求版本匹配并递增，删除为逻辑隐藏。删除前同时检查当前 `member_account` 和不可变 `member_level_log`，已使用等级拒绝删除，保护用户归属及历史审计。 |
| 配置完整性 | 已实现（代码） | 规则要求为非空 JSON 对象，权益为非空、去重、长度受限的 JSON 字符串数组；排序等级由数据库唯一约束保护。已删除等级会从用户等级调整候选列表排除。 |
| RBAC 与中文模拟数据 | 已实现（代码） | 仅平台角色且持有 `user.member.level.manage` 可访问；夹具含“普通会员、悦享会员、尊享会员”及中文规则、权益，保持 utf8mb4。 |
| 隔离运行时验收 | 已关闭（本地假凭据） | 平台角色的配置与候选等级接口均为 HTTP 200；商户、区域、客服、运营对配置、候选与调整命令均为 HTTP 403。已验证重复排序 409、版本冲突 409、已绑定/历史引用等级拒绝删除 409、逻辑删除等级拒绝调整 404。Vben 中文页面展示“普通会员、悦享会员、尊享会员”，并已修复登录后直达深层动态菜单错误落入 404 的问题。 |
| 调整幂等与资产边界 | 已关闭（本地假凭据） | 对虚构用户完成“悦享会员→尊享会员→普通会员”闭环：相同幂等键重放不新增日志，不同请求重放返回 409；库内保留两条不同幂等键的不可变日志。复核积分 268、余额 ¥36.50、佣金 ¥0.00 不变，未写入 SVIP、优惠券或订单事实。 |
| 全局生产验收边界 | 未关闭 | 本条只关闭普通会员等级配置与人工调整的隔离运行时证据；支付、退款、库存、优惠券、积分、佣金、结算及其余 CRMEB 基线仍须逐项完成状态机、数据范围和运行时验收。 |

## 2026-08-04：SVIP 会员权益主数据与套餐关联

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| CRMEB 基线补齐 | 已实现（代码） | 新增“会员权益”真实 Vben 页面，覆盖权益列表、新增、编辑、启停和逻辑删除；“会员类型”页面由自由文本改为只可勾选已启用权益。付费会员协议仍由统一“协议规则”页中的 `sys_svip` 配置管理。 |
| 关联与快照边界 | 已实现（代码） | 新建或编辑套餐时，后端校验所有权益均存在、启用且未删除；删除权益时检查启用套餐 JSON 快照，仍被使用则拒绝。已生成订单保留套餐名、类型、金额、时长快照，后台不修改订单或权益发放事实。 |
| 并发、RBAC 与角色收口 | 已实现（代码） | 权益主表使用唯一名称、`version` 条件更新和 `deleted_at` 逻辑删除；平台/运营需 `user.svip.interest.manage`。运营仅保留会员类型、权益、记录菜单；已收回无后端写权限的手工 SVIP 用户状态入口，避免菜单可见但接口拒绝。 |
| 中文模拟数据与自动化 | 通过静态验证 | 夹具提供“会员专享价、专属客服优先响应、每月权益提醒、年度权益礼包”等 utf8mb4 数据；`api-platform go test ./internal/platform/svip ./internal/platform/svipinterest ./internal/pkg/middleware ./cmd`、`admin-platform pnpm run build:platform` 通过。 |
| 隔离运行时验收 | 已关闭 | 已验证平台/运营允许、商户/区域/客服拒绝；未引用权益可新增、编辑、逻辑删除，旧版本更新返回 409；已引用权益删除返回 409，停用权益被套餐引用时保存返回 400。 |

## 2026-08-04：平台优惠券发送记录

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| CRMEB 基线补齐 | 已实现（代码） | 新增“优惠券发送记录”真实 Vben 页面，覆盖 CRMEB 平台优惠券的发送记录查询：按用户、优惠券和发放/撤销操作筛选、分页展示。该页只读，不以静态占位页替代审计事实。 |
| 审计与资金边界 | 已实现（代码） | 数据来自 `qixi_crm_b_user_coupon_command_audit` 的不可变人工发券/撤销命令；不返回幂等键、账号、手机号或其他个人资料。页面不允许编辑、删除、补写或重放，也不处理订单锁券、支付、核销或券生命周期写入。模板被删除后记录仍保留。 |
| RBAC 与角色隔离 | 已实现（代码） | 接口严格限平台角色，并要求 `marketing.coupon.send.read`；商户、区域、运营、客服均无菜单和接口权限。原发券/撤销命令继续要求独立的 `user.coupon.manage`。 |
| 中文模拟数据与自动化 | 通过定向静态验证 | 业务夹具新增“补发夏日活动体验券”“活动资格调整，撤销未使用体验券”等 utf8mb4 虚构审计数据，未伪造支付、核销或真实用户资料。`api-platform go test ./internal/platform/userlist ./internal/pkg/middleware` 与 `admin-platform pnpm run build:platform` 已通过。 |
| 隔离运行时验收 | 已通过（本地假凭据） | 平台虚构账号查询到 2 条发送审计；`action=revoke` 筛选返回 1 条。Vben 页面截图确认中文原因、状态变化和只读边界。商户、区域、运营、客服虚构账号访问发送记录均被服务端拒绝（HTTP 403）。 |
| 生产验收边界 | 未关闭 | 仍须验证已删除模板回退展示及发券命令与订单、核销状态机回归；不以本条代表营销域或全后台生产验收完成。 |

## 2026-08-04：平台优惠券领取与使用记录

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| CRMEB 基线补齐 | 已实现（代码） | 新增“优惠券领取记录”真实 Vben 页面，覆盖 CRMEB 平台优惠券使用记录的用户券查询：按用户、优惠券、未使用/锁定/已使用/已过期状态筛选与分页展示。 |
| 状态机边界 | 已实现（代码） | 列表只读 `qixi_crm_b_coupon_user` 事实，不做人工核销、解锁、删除或状态修复；锁券、支付核销、取消恢复和过期规则仍由订单与券状态机唯一写入。模板删除后，历史用户券仍保留并标识为已删除模板。 |
| RBAC 与最小化数据 | 已实现（代码） | 接口严格限平台角色并要求 `marketing.coupon.record.read`；仅返回用户 ID、券、来源、状态、关联订单 ID 与时间，不读取账号、手机号或地址。商户、区域、运营、客服无相应菜单与接口权限。 |
| 中文模拟数据与自动化 | 通过定向静态验证 | 复用 utf8mb4 中文优惠券夹具及未使用、已使用、已过期三种用户券状态，不伪造真实订单、支付或个人资料。`api-platform go test ./internal/platform/userlist ./internal/pkg/middleware` 与 `admin-platform pnpm run build:platform` 已通过。 |
| 隔离运行时验收 | 已通过（本地假凭据） | 平台虚构账号查询到未使用、已使用、已过期共 3 条用户券；`status=used` 筛选返回 1 条。Vben 页面截图确认状态标签、订单关联与只读边界。商户、区域、运营、客服虚构账号访问领取记录均被服务端拒绝（HTTP 403）。 |
| 生产验收边界 | 未关闭 | 仍须验证订单锁券、支付核销、已删模板回退与券状态机回归；不以本条代表券域或全后台生产验收完成。 |

## 2026-08-04：统一后台操作日志

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| CRMEB 基线补齐 | 已实现（代码） | 新增“系统设置 / 操作日志”真实 Vben 页面与筛选、分页查询，覆盖 CRMEB `维护 / 操作日志` 的只读职责。 |
| 审计边界 | 已实现（代码） | 统一后台成功的 POST、PUT、PATCH、DELETE 请求由中间件记录管理员 ID、角色、路由资源、资源 ID、请求号和时间；不记录请求体、密码、令牌、手机号或个人资料。领域状态机仍以各领域不可变审计为准。 |
| RBAC | 已实现（代码） | 日志查询只允许平台角色，并要求 `setting.operation_log.read`；商户、区域、运营、客服均无菜单和接口权限。 |
| 中文模拟数据与自动化 | 通过全量后端与前端静态验证 | `sql/admin/05_test_data.sql` 提供两条中文业务语义的虚构操作日志，不初始化真实后台账号、密码或凭据。`api-platform go test ./...`、`admin-platform pnpm run build:platform` 已通过。 |
| 隔离运行时验收 | 已通过（隔离双库 HTTP 与真实 Vben） | `TestCustomerServiceHTTPRBACAndQueueClosure` 使用虚构中文客服执行真实 `DELETE /setting/admins/:id`：仅成功的平台删除写入一条 `setting/987672006` 操作审计，包含平台角色、路由模板与非空请求号；商户、区域、运营、客服的失败删除均不落审计。平台可按 action 查询该行，四种非平台角色读取操作日志均为 403；审计响应不含测试密码散列或请求体。另在一次性本地 Vben 环境发起成功的客服设置更新，操作日志页实际显示 `PUT /api/platform/v1/customer-service/settings`、`customer-service`、固定虚构请求号与 1 条结果，内联截图已复验。夹具清理后账号、会话、审计均为 0。 |
| 生产验收边界 | 未关闭 | 本项关闭真实成功写操作落库、失败操作不落库、五角色读取拒绝和清理证据；全局审计的其余写域与全后台功能仍须逐项验收，不能据此宣称全量完成。 |

## 2026-08-04：平台订单发票监管

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| CRMEB 基线补齐 | 已实现（代码） | 新增“财务管理 / 发票管理”真实 Vben 页面，以及发票列表和详情 API，覆盖 CRMEB 平台发票列表、详情的只读监管职责。 |
| 事实与隐私边界 | 已实现（代码） | 查询 `qixi_crm_b_order_invoice` 与订单快照；响应仅返回脱敏税号、脱敏邮箱、订单、店铺、状态与发票号，不返回发票文件地址。平台不提供人工开票、改号、改文件、作废或删除，避免越过业务开票事实链。 |
| RBAC | 已实现（代码） | 仅平台角色且持有 `accounts.invoice.read` 可查询，商户、区域、运营、客服无菜单和接口权限。 |
| 中文模拟数据与自动化 | 通过静态验证 | 业务夹具提供“CRM Live演示科技有限公司”等 utf8mb4 虚构发票记录，税号与邮箱为无效演示值。`api-platform go test ./internal/platform/invoice ./internal/pkg/middleware ./cmd` 与 `admin-platform pnpm run build:platform` 通过。 |
| 隔离运行时验收 | 已通过（本地假凭据） | 独立 MySQL 8.4.10 导入中文夹具后，平台虚构账号可查询 1 条发票；Vben 页面确认税号为 `**************345X`、邮箱为 `f***@invoice.invalid`，且页面仅提供只读查询。商户、区域、运营、客服虚构账号访问发票接口均被服务端拒绝（HTTP 403）。 |
| 生产验收边界 | 未关闭 | 仍须验证状态与订单号筛选、详情不存在拒绝和跨库订单事实回归；不以本条代表财务域或全后台生产验收完成。 |

## 2026-08-04：统一后台登录安全审计

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 审计闭环 | 已实现（代码） | 统一后台登录成功、账号不存在、账号禁用、密码错误和角色签发失败均非阻塞写入 `qixi_crm_a_login_log`。 |
| 隐私与可用性边界 | 已实现（代码） | 审计只保存账号标识、成功标志、角色、IP、受限长度客户端标识与时间；绝不保存密码、令牌或请求体。日志写入失败不会导致正常登录失败。 |
| 中文模拟数据与自动化 | 通过定向验证 | 提供“本地验收浏览器”等 utf8mb4 虚构登录日志；`api-platform go test ./internal/admin/auth ./cmd` 通过。 |
| 日志查询与 RBAC | 已实现（代码） | 新增“系统设置 / 登录日志”真实 Vben 页面和 `/login-logs` 查询接口；仅平台角色且具有 `setting.operation_log.read` 可查看，商户、区域、运营、客服无菜单及接口权限。 |
| 隔离运行时验收 | 已通过（本地假凭据） | 五个虚构角色均可完成统一后台登录；平台安全审计账号在 Vben 页面可读到 8 条登录日志（含 5 个本次角色登录），日志不含密码、令牌或请求体。商户、区域、运营、客服访问登录日志接口均被服务端拒绝（HTTP 403）。 |
| 生产验收边界 | 未关闭 | 仍须验证失败登录落库、IP 解析与角色长度边界；不以本条代表安全审计或全后台生产验收完成。 |

## 2026-08-04：运营用户充值计划与订单监管

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| CRMEB 基线补齐 | 已实现（代码） | 新增充值计划列表、新增、编辑、启停与充值订单状态筛选；Vben 页面统一展示计划和只读订单。 |
| 资金边界 | 已实现（代码） | 后台仅维护未来可售计划，不提供删除或任何充值订单、回调、余额流水写入；已支付订单、渠道回调幂等和余额入账继续由业务服务事务负责。 |
| RBAC | 已实现（代码） | 路由仅限 `platform`/`operations` 且要求 `marketing.recharge.manage`；运营白名单已加入 `/recharge/`，商户角色拒绝测试覆盖。 |
| 中文模拟数据与自动化 | 通过静态验证 | 业务夹具已有“100 元充值”“300 元充值”等 UTF-8 中文方案；`api-platform go test ./internal/platform/recharge ./cmd`、`admin-platform pnpm run build:platform` 通过。 |
| 生产验收边界 | 未关闭 | 尚未在隔离假凭据 MySQL 演练计划停用后的新订单拒绝、已支付订单不可变、回调重放与五角色截图；不以本条代表充值资金域或全后台生产验收完成。 |

## 2026-08-04：运营积分商城商品与订单监管

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| CRMEB 基线补齐 | 已实现（代码） | 新增积分商城菜单、积分商品列表/统计/编辑及积分订单只读列表；复用业务库 `qixi_crm_b_points_product_view`、`qixi_crm_b_group_order(activity_type=20)` 和既有积分下单事务。 |
| RBAC 与范围 | 已通过本地运行时验收 | 仅 `platform`、`operations` 角色且持有 `marketing.points.manage` 按钮码可访问；真实注册路由中运营更新为 HTTP 200、平台旧版本更新为 HTTP 409，商户、区域、客服均为 HTTP 403。运营路由白名单显式纳入 `/points/`；商品列表支持商户 ID、名称和状态筛选，非法或零商户 ID 拒绝。 |
| 订单快照、状态机与并发 | 已通过本地运行时验收 | 商品更新仅允许积分、库存、上下架，并要求当前 `version`；条件更新成功后递增版本，过期写入返回 409。订单关闭状态统一为业务库 enum 的 `closed`（不使用不存在的 `cancelled`）；已关闭订单筛选实际返回对应订单。已存在订单的应付积分 240、件数 2 与 `closed` 状态在商品更新后保持不变。 |
| C 端边界与中文模拟数据 | 已通过本地运行时验收 | 业务服务以 UTF-8 中文虚构积分商品完成报价，商品下架后新建报价返回 `errNotFound`；平台商品更新不会为旧订单重算积分。页面夹具“中文积分页面商品/中文积分验收商户”字节核验均为 utf8mb4，未使用真实个人或支付数据。 |
| Vben 页面、自动化与清理 | 已通过本地运行时验收 | 平台虚构账号真实登录 Vben，在“积分商城监管”将中文商品从 120 积分/库存 8 更新为 130/7；“已关闭”筛选显示只读订单，浏览器控制台无 error。`go test ./internal/platform/points`、`go test ./internal/business/points`、`pnpm run build:platform` 与差异检查在本轮最终回归；账号、商品、订单及临时服务均在验收后清理。 |
| 全局生产验收边界 | 未关闭 | 本条只关闭积分商品监管、订单只读快照与下架报价拒绝的隔离闭环；积分扣减、支付、退款、库存预占/回补、积分流水与跨服务事件仍须在各自状态机中独立验收，不能据此宣称全后台已生产验收完成。 |

## 2026-08-03：用户反馈监管准备

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 业务审计基础 | 已实现（数据模型） | 新增 `qixi_crm_b_user_feedback_audit`，为平台回复、关闭、幂等重放保留原状态、目标状态、回复、操作人及唯一幂等键；不覆盖 C 端原始反馈。 |
| API、RBAC 与页面 | 已实现（代码） | 平台新增 `/user-feedback` 列表和回复/关闭/删除命令；`platform/customer_service` 角色以 `user.feedback.read`、`user.feedback.manage` 区分读取与写入，客服种子菜单已授予“用户管理 → 用户反馈”，角色总闸也只额外放行该反馈路由。Vben 拆分“反馈列表、反馈分类”页面，支持状态筛选、分页、回复、关闭和软删除；不返回手机号等个人资料。 |
| 状态机与命令 | 已实现（代码） | NATS 命令仅允许 `pending → replied` 与 `pending/replied → closed`；业务事务同时更新反馈和审计，回复、关闭均有全局幂等键。 |
| 分类、删除与中文模拟数据 | 已实现（代码） | 已新增反馈分类的新增、编辑、启停、软删除及独立审计；反馈列表支持软删除，分类快照不因后续改名或删除而改写。`sql/business/05_test_data.sql` 提供“功能建议、订单问题、使用体验、历史分类”及三条 UTF-8 中文反馈夹具。 |
| 生产验收 | 未关闭 | 尚缺隔离 MySQL/NATS 的真实往返、幂等冲突/断链恢复、五角色授权截图，以及 C 端分类选择和平台分类维护的浏览器截图。 |

## 2026-08-03：平台商品标签、保障与参数模板

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 数据边界 | 已实现（代码） | 平台商品标签、保障服务和参数模板改为 `qixi_crm_a_product_label`、`qixi_crm_a_product_guarantee`、`qixi_crm_a_product_parameter_template`；`api-platform` 启动入口已移除旧 `qixi_m_admin_store_product_*` 仓储依赖。店铺私有元数据仍归 `api-merchant`。 |
| API 与 RBAC | 已实现（代码） | 三组 `GET/POST/PUT/DELETE` 路由均只允许 `platform`，并分别要求 `product.label.manage`、`product.guarantee.manage`、`product.parameter.manage` 按钮码。名称唯一、状态仅 `0/1`；参数值必须非空、去重且最多 50 项。 |
| Vben 页面 | 已实现（代码） | `/product/label`、`/product/guarantee`、`/product/specs` 均指向真实元数据工作台，可新增、编辑、删除、启停和展示中文参数值；未注册路径不会降级为占位页。 |
| 中文模拟数据与自动化 | 通过静态校验 | `sql/admin/05_test_data.sql` 提供“七天无理由”“正品保障”“演示规格颜色”等 utf8mb4 虚构数据；元数据规则单测、`api-platform go test ./...`、`admin-platform pnpm run build:platform`、`git diff --check` 通过。 |
| 生产验收边界 | 未关闭 | 尚未在隔离三库中导入夹具，以平台和无权限角色分别验证 CRUD、唯一冲突、参数值边界和页面截图；商品与店铺的标签/保障投影关系也须在对应商品履约闭环中继续验证。 |

## 2026-08-03：平台商品评论审核

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 数据归属与跨库边界 | 已实现（代码） | 评论事实始终保留在业务库 `qixi_crm_b_product_comment`；平台只读评论投影。审核、虚拟评论、排序和删除均通过 NATS 请求/响应发送至 `api-business`，由业务服务在本库事务内更新，平台不跨库写评论。 |
| 状态机与幂等 | 已实现（代码） | 仅允许 `pending/hidden → published` 与 `pending/published → hidden`；虚拟评论可创建、编辑、排序和软删除，真实用户评论不能被编辑或删除。每次命令写入审计表并以全局唯一幂等键保护。 |
| 权限与最小数据 | 已实现（代码） | 读审与展示使用 `product.comment.review`；虚拟评论维护、排序、删除分别要求独立按钮码。响应不包含用户手机号、昵称、收款资料或媒体敏感地址。 |
| Vben 页面与受控图片 | 已实现（代码） | `/product/comment` 提供筛选、分页、展示/隐藏；虚拟评论明确标识来源并可新增、编辑、排序、软删除。图片仅能由现有平台素材选择器选择，平台验证素材 ID 为平台归属的图片后才传给业务服务；未重新选择图片的编辑不会清空既有图片。真实评论界面不出现篡改或删除操作。 |
| 中文模拟数据 | 已提供 | `sql/business/05_test_data.sql` 包含“羊绒柔软，版型合身”“香氛淡雅，包装完整”及显著标识来源的“演示用户小满”虚构评论；全部为 utf8mb4 中文夹具。 |
| 自动化与构建 | 通过 | 平台命令客户端、业务状态机、平台路由包与两个启动入口定向 Go 测试通过；新增图片数量上限、空素材、无损编辑语义的单元测试；`admin-platform pnpm run build:platform`、`git diff --check` 通过。 |
| 生产验收边界 | 未关闭 | 尚未建立隔离 MySQL/NATS 假凭据运行时执行消息往返、并发重放、五角色拒绝/允许矩阵和浏览器截图；不得据此将评论模块或全后台标为生产验收完成。 |

## 2026-08-04：客服工作台队列与快捷回复

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| CRMEB 基线与职责 | 已通过隔离运行时验收 | 统一 Vben 客服工作台覆盖客服队列、领取、转接、事件投影、订单辅助、客服人员/服务用户、快捷回复、用户备注与客服设置。聊天正文、UserSig 与 WebSocket 仍由 pte-live-im 承担，后台不伪造或落库聊天正文。 |
| 五角色与数据范围 | 已通过本地 MySQL 运行时验收 | 真实 JWT、会话版本、角色总闸和区域限制串联：平台可更新客服设置；商户、区域、运营、客服写设置均为 HTTP 403。客服仅可读写 `service_queue` 授权店铺；范围外会话详情/用户备注为 HTTP 404、范围外快捷回复新增为 HTTP 403。 |
| 转接状态机与幂等 | 已通过本地 MySQL 运行时验收 | 同店铺已启用客服才可作为目标；领取后转接写入一条不可变分配审计。同一操作人与同一键重放返回既有结果且不重复审计，改变目标或中文原因复用键返回 HTTP 409；其他操作人不能利用该键重放。 |
| Vben 与中文模拟数据 | 已通过本地页面验收 | 虚构“七禧客服页面演示店”“中文客服用户小满”“中文客服转接目标”在 UTF-8 隔离库完成领取、中文原因转接以及快捷回复“新增→编辑/停用→软删除”。修复无服务范围历史账号返回 `null` 导致前端转接筛选异常：API 现在稳定返回空数组，前端亦有兼容兜底；新浏览器会话控制台无 warning/error。 |
| 生产验收边界 | 未关闭 | 本条关闭客服后台队列/快捷回复的本地闭环，不替代 pte-live-im 远端 E2EE 文本消息、多 AppId、真实客服坐席投影和外部 IM 连通性验收。 |
