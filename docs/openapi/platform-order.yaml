openapi: 3.0.3
info:
  title: qixi-live-ecrm platform order
  version: 0.3.0
  description: 阶段 3 平台订单监管（只读）
servers:
  - url: /api/platform/v1
paths:
  /orders:
    get:
      summary: 平台子单列表（区域管理员仅返回其 region_ids 内商户订单）
  /orders/{id}:
    get:
      summary: 子单详情（受区域数据范围限制）
