# app-harmony — 七禧鸿蒙客户端

独立 OpenHarmony 工程目录。目标为 OpenHarmony API 23、ArkTS、ArkUI、MVVM/Repository；不得以 `app-uni` WebView 替代。

与 `app-pc`、`app-uni`、`app-ios`、`app-adnroid` 共用 `api-business` 的 C 端业务契约和 JWT；用户登录来源标识为 `harmony`。导航栏高度 44px，紧贴状态栏底部。

当前仅完成目录收敛，尚未创建 Harmony 工程或宣称功能完成。
