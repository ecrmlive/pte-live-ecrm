---
description: 每次回复开头标明当前使用的大模型，便于用户第一时间核对
alwaysApply: true
---

# 回复开头必须打印当前模型

每次对用户回复（含续写、工具回合后的最终回复）**第一行**必须标明当前会话实际使用的模型，格式固定：

```text
【模型】<常用名>（<slug 或路由名，已知则写>）
```

## 常用名（与 agent-model-policy 一致）

- Claude Opus 5
- GPT-5.6 Terra
- Cursor Grok 4.5 High Fast
- 若为 Auto 路由 / Composer / 其它非白名单：如实写出，例如 `【模型】Auto（Composer 路由）`，**不要谎称**为白名单三者之一

## 要求

1. 放在回复最顶部，单独一行，后面空一行再写正文。
2. 不知道精确型号时写清已知信息，禁止编造版本号。
3. 子代理 / Task 另有模型时，在委托说明或该代理首条可见输出中同样标明。
4. 不因此规则省略对用户问题的直接回答。
