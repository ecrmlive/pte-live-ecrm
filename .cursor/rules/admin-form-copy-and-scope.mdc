---
description: 管理后台页面范围、字段文案与保存动作必须以用户确认内容为准
globs: admin-platform/src/**/*.{vue,ts,tsx},admin-merchant/src/**/*.{vue,ts,tsx}
alwaysApply: true
---

# 管理后台：功能范围与保存动作强制约束

适用于 `admin-platform`、`admin-merchant` 全部页面。

## 功能范围

- 页面字段、功能、产品文案只能来自用户提供的截图、用户明确需求或仓库已确认的功能文档。
- 用户截图是页面字段、顺序、操作项与可见文案的最高验收依据；未出现且未明确要求的字段、策略、开关、说明和业务扩展一律不得自行添加。
- 信息不足时，先保留截图已有内容或向用户确认；禁止以技术实现习惯、旧页面字段或推测补充功能。
- 面向用户仅保留产品级文案；禁止展示技术实现、内部机制或开发说明，除非用户明确要求。
- 店铺、用户、商品、客服、商户、分类、标签等关联实体必须通过受权限约束的选择器、关联弹窗或列表选择关联，禁止让用户手工输入 ID、编号、逗号分隔 ID 或数据库键值；关联字段仅在业务确有归属需求时展示。
- 新增和编辑必须使用同一套关联选择交互；如业务禁止更换关联对象，须在产品需求中明确说明并由后端校验。不得因接口未实现而退回为手输 ID。
- 代码层禁止用 `ElInput`、`ElInputNumber` 或 textarea 直接绑定 `*_id`、`*_ids`、`*Id`、`*Ids` 这类关联字段。统一复用关系选择组件或业务选择器，并从权限范围内的接口加载候选项。

## 新增、编辑表单（强制）

- 所有新增、编辑类页面、抽屉、弹窗必须只使用两种操作组合：**取消、保存**，或需要恢复初始值时使用 **重置、保存**。
- 两个按钮必须在操作区水平居中显示，顺序固定为：取消/重置在左，保存主按钮在右。
- 新增、编辑等会写入数据的表单，主操作按钮文本必须统一为 **保存**。
- 禁止使用 **提交**、**确定**、**完成**、**保存并发布**、**保存设置** 等替代文本，除非用户在当前需求中明确指定。
- 使用 `useVbenDrawer` 时必须设定 `cancelText: '取消'`、`confirmText: '保存'`，并使用居中操作区；普通表单/弹窗也必须遵循相同按钮文本、顺序和居中布局。
- 选择器、删除确认、审核、发货、撤销等非新增/编辑表单不属于本条，不得把其业务操作文案误改为“保存”。
- 设置页以用户截图或明确需求为准；若无特别说明，主操作文本使用“保存”。

## 搜索区域（强制）

- 管理后台列表筛选操作仅保留两个按钮，顺序固定为：**重置、搜索**。
- 必须使用 `listFormOptionsDefaults(schema)` 或显式设置等价配置：重置按钮为“重置”、提交按钮为“搜索”、不得设置 `actionButtonsReverse: true`。
- 搜索区域不得使用“查询”“提交”“确定”等替代文案，除非用户在当前需求中明确指定。

## 新增与编辑命名（强制）

- 工具栏新增按钮、抽屉/弹窗标题必须采用 **新增XXX**、**编辑XXX** 的完整命名，XXX 为当前业务对象。
- 所有创建动作的按钮、菜单、抽屉/弹窗标题一律使用“新增XXX”；禁止使用“添加XXX”或“添加数据”。“添加时间”等业务字段名、说明性正文不受此条限制。
- 禁止使用“编辑数据”“新增”“编辑”等缺少业务对象的泛化文案。
