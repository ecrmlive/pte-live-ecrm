# 本机 go run：cp conf/admin.local.yaml.example conf/admin.local.yaml
# go run ./cmd/api-admin -config conf/admin.local.yaml
# COS 密钥优先用环境变量 QIXI_COS_*，勿把真实密钥写进文件。
server:
  addr: ":8080"
  mode: debug

mysql:
  dsn: "qixi:qixi_local@tcp(127.0.0.1:13306)/qixi_mergers?charset=utf8mb4&parseTime=True&loc=Local"

redis:
  addr: "127.0.0.1:16379"
  password: ""
  db: 0

nats:
  url: "nats://127.0.0.1:14222"

etcd:
  endpoints:
    - "127.0.0.1:12379"

cos:
  enabled: true
  bucket: ""
  region: "ap-guangzhou"
  secret_id: ""
  secret_key: ""
  base_url: "https://cos.qxkejiwl.top/qixi-mergers"
  key_prefix: "qixi-mergers"

jwt:
  secret: "qixi-mergers-dev-jwt-secret-change-me"
  access_ttl_hours: 12
  refresh_ttl_hours: 168

upload:
  dir: "data/uploads"
  public_base: "/uploads"
